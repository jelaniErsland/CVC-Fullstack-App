# 12.47 production readiness — updated 2026-09-24

## September 24 read-only release gate

The fresh production inspection found the exact Bozeman project/ref, 39 migrations ending at `20260908130000`, and exactly the four reviewed 12.47 migration files pending locally. The public catalog matches the deployed 58-signature fixture exactly (8 anonymous / 38 authenticated / 12 internal), including owners, pinned empty search paths and per-signature grants. PUBLIC EXECUTE and reviewed creator-default PUBLIC/anon/authenticated EXECUTE remain zero. All 18 public tables have RLS, with no PUBLIC table grants or unexpected anonymous data access. Storage still has zero buckets and zero objects. No production mutation occurred.

The September 24 aggregate baseline is 103 active/total volunteers, 77 Calendar items, 23 assignments, 23 responses, 26 schedule credentials, zero response tokens, 18 notification deliveries, 11 Task presets, nine Auth users/identities, nine active contacts and nine active workspace grants. Inspected relationship/orphan and unvalidated-FK counts are zero. These read-only observations were taken across separate queries while contacts could still edit data; they are **not an atomic migration-window snapshot**. Counts match the preceding snapshot; one assignment has a later `updated_at` value. The fresh row digests use a newly specified `jsonb_agg(to_jsonb(row) ORDER BY id)` calculation, so they are **not comparable** with the earlier differently generated digests. During an authorized quiet window, capture a fresh repeatable-read fingerprint with this same method immediately before the checkpoint; investigate any later change, without treating a historical roster count as fixed.

Privacy-preserving September 24 MD5 row digests (the database computed the digests; no private row values were returned): volunteers `a27800a07e66558dee1c8ee5071a6594`, Calendar `c31b021924b9d52ebab3e4c99372869f`, assignments `5ee8e858e2ea5ee61f2302c92d4fd4ac`, responses `8a13e2ee4e119f8ed7d3a90b71829b45`, schedule credentials `30a78ff8fb7b92c1ab055f20fb1d92f9`, notifications `880f65445e1d0fd3eec9ee76179c1f98`, contacts `be2978163821f7d8ffa02afedcd1d7bc`, grants `9cac1bb6ec140dac0da4a5cb9bbaf076`. Auth user-ID and identity-ID digests were `0d160ab769703d0d83bbbb4324ba9c70` and `b2b84901461cffbfd017c94c812f116d`. These fingerprints are comparison aids, not evidence that writes have stopped.

The permanent task is Enabled/Ready/not running, locked at `20260908130000`, with the reviewed daily 03:15 action, Interactive/Limited principal and result zero for its September 24 run. Read-only recovery preflight passed. The latest independently checked successful encrypted artifact is from 2026-09-24 09:15 UTC, 129,106 bytes; its size and SHA-256 match the safe status record. Plaintext, partial and temporary residue counts are zero. This is artifact integrity, not a new restore rehearsal. The previously completed two-target real-backup restore remains the recovery proof.

At the September 24 preflight, `origin/master` matched source baseline `de667eea158df44d99e7b31d95a089cb7440c5b4`, and the public site returned HTTP 200. The owner subsequently supplied a Vercel deployment-details screenshot showing Production/Ready from `master` at prefix `de667ee`. This supports the expected baseline prefix, but the full deployed SHA was not independently obtainable through authorized read-only metadata. Confirm the full deployed SHA before the live rollout; a screenshot prefix and public HTTP response are insufficient. The owner-observed configuration inventory below is setting-presence evidence, not proof of delivery.

The exact source release manifest was reconciled against the September 24 precommit worktree: 131 listed paths, 131 source dirty paths, no missing/unlisted source path, zero staged files, and 69 separate review captures excluded from the commit. It includes the complete public guide, four migrations, exact 37-signature grant reconciliation, backup-lock helpers, tests and documentation. `.local`, `.next`, `supabase/.temp` and `.env.local` are outside scope. The focused guide route was repaired by restoring its public HTML base path without changing the owner's visual design.

**Decision: local release preparation may complete; production rollout still requires full Vercel baseline-SHA confirmation, a fresh quiet-window preflight and separate authorization.** The reduced-scope database/Auth/empty-Storage recovery proof and synthetic-fixture Next.js route smoke are complete locally. The owner has confirmed account, MFA, domain/DNS, credential-reissuance and independent age-identity custody arrangements; these arrangements have not been exercised as a full external-platform disaster recovery. The four forward migrations remain unapplied. Production was not mutated in this preparation; no backup task was started or changed and no email was sent. See [the exact platform recovery contract](12_47_AUTH_STORAGE_RECOVERY.md).

## Evidence classes and source baseline

| Class | Evidence |
| --- | --- |
| Verified live, read-only | Supabase migration catalog, function/RLS/grant catalog, aggregate operational fingerprints, Task Scheduler status/action, encrypted artifact size and independent SHA-256. Snapshot taken 2026-09-23 19:59:49 UTC. |
| Verified local / isolated | Fresh local migration replay and synthetic fixture; decryption and **Project Local-scoped** restore of the verified real encrypted artifact; four migrations over its real public data; 18 old-column fingerprints preserved; exact 72-function ACL/RLS proof; encrypted expanded-schema backup and independent scoped restore with 26 table fingerprints matching. |
| Not verified | Live Vercel email environment; current production application behavior after 12.47 (not deployed); off-host project-photo BLOB recovery; real-contact login/email after a disaster. Complete Auth identity and current empty Storage metadata recovery have now been proven locally in two platform-initialized targets. |

| Repair gate | Result |
| --- | --- |
| 1. Exact function ACL | **PASS locally** after the 37-signature correction and fresh rebuild; [per-function audit](12_47_ACL_RECONCILIATION.md). The exact amendment is included in the reviewed local release scope. |
| 2. Actual backup restoration | **PASS for the reduced-scope Project Local application, essential Auth users/identities, current empty Storage metadata, migrations and exact ACL/RLS.** Compatible platform definitions came from pinned Supabase CLI `2.111.0`, PostgreSQL `17.6.1.156`, GoTrue `v2.194.0` and Storage `v1.67.20`; a second fresh target restored an encrypted expanded package independently. Existing sessions/OTP state is intentionally excluded; contacts sign in again. Photo BLOB recovery is outside this gate. |
| 3. Four backup-lock transitions | **PASS locally** for all four exact adjacent transitions and fail-closed fixtures. No live task action occurred; the helper changes are included in the reviewed local release scope. |

The production baseline HEAD before the local release branch was `de667eea158df44d99e7b31d95a089cb7440c5b4` on `master`. The reviewed 12.47 release scope includes source, tests, documentation and the public guide; review captures are excluded. At the September 24 scope review, staged files were zero. The apparent 127-to-70 dirty-path drop was only Git presentation: `git status --short` collapses untracked directories, while `--untracked-files=all` enumerates them. Before the two readiness documents there were 127 individual paths; afterward 129; the exact current path inventory is maintained in the release-scope manifest and must be refreshed after any source edit. All four migrations, final assignment UX, Communications, CSV, volunteer home and photo implementation remain present. All 60 reviewed PNG captures remain: 36 local-review, 20 UX-consolidation and 4 final-polish. The only pending migration filenames and order remain `20260922120000_bulk_calendar_assignments.sql`, `20260922130000_communication_delivery_operations.sql`, `20260922140000_volunteer_csv_import.sql`, and `20260922150000_project_hero_volunteer_home.sql`. No applied migration was changed. Runtime `.local`, `.next` and `supabase/.temp` are ignored; inspect release scope before any future production deployment.

## September 23 production baseline (historical comparison)

The database has 39 migrations, terminal `20260908130000`, and none of the four pending versions. The permanent backup task is Enabled/Ready, not running, with action lock `20260908130000`, Interactive/Limited principal and reviewed action. Its most recent task result is zero. Recovery's migration/lock checks are aligned; this alone does not prove restoration.

The latest status inspected for this rehearsal reports a successful scheduled encrypted artifact at 2026-09-23 09:15:19 UTC: `project-local-production-20260923T091518Z-c546d51c.zip.age`, 128,747 bytes. Its SHA-256 independently matched status. The owner-located existing age identity at the documented local RecoveryKey path decrypted it without disclosing key or data. The six-member archive and all five dump lengths matched the encrypted manifest, whose `ExpectedMigration` is `20260908130000` and source commit is `de667ee`. This proves artifact access and integrity, not complete platform recovery.

The one active workspace is **Bozeman Major Remodel**, timezone America/Denver, project dates 2026-09-29 through 2026-12-04. Its ID is represented here only by MD5 fingerprint `d9fedc482080f8bd1848300c29d9fa25`. At the snapshot: 103 active/total volunteer profiles, 77 Calendar items, 23 assignments, 23 responses, 26 schedule credentials, 18 notification deliveries, 11 task presets, 9 Auth users, 9 project contacts and 9 workspace grants. The observed roster is current data, not a future rollout invariant. Six groups share an active volunteer email; Communications must continue excluding ambiguous addresses for explicit review. No orphan assignment, response, credential, notification, contact or grant relationship was found in the inspected relations. No private profile values, email addresses or token contents are recorded here.

For a future point-in-time comparison, the read-only snapshot produced these existing-row digests (MD5 of canonically ordered row JSON): volunteer profiles `a7fb23e2b4c475b637050e1a1dbb8f64`, Calendar items `3d30f4dc8806bd98782f6f0d061a4cde`, assignments `80e8b704a6d3bca5ea3a1e1719434b05`, responses `55a58a3b34c11679485763bd3f0aae14`, schedule credentials `7446ac17d390bc53232455b93fbaf108`, notification deliveries `8a5cbeaebe6e42c4c0504b5c04f4e195`, contacts `4e5f6555f801227a95eb2787edeebf49`, grants `da90b0403e0e6eced19efe6906cd8099`. These are comparison aids, not secrets and not substitutes for a frozen migration-window fingerprint. Recompute after pausing writes immediately before migration; live volunteer editing means historical counts/digests must never be hard-coded as acceptance values.

Live public function inventory is exactly 58: 8 anonymous, 38 authenticated-only and 12 internal. `PUBLIC EXECUTE=0`; default PUBLIC/anon/authenticated EXECUTE grants=0 (the hosted service_role default remains granted); all 46 SECURITY DEFINER functions have expected owner `postgres` and pinned empty `search_path`. The exact live signatures and grants match the 58-signature pre-12.47 subset of the source fixture. All 18 inspected public tables have RLS enabled. Production storage has zero buckets and objects. Existing project-photo uploads remain disabled in source on Vercel and in the new migration's default flag. Live Vercel email-related environment values were **not** independently verified; previously documented enablement is not fresh proof.

## Migration review and compatibility

| Order | Semantic change and preservation assessment |
| --- | --- |
| `20260922120000` | Adds a durable bounded bulk-assignment operation and transactional preview/save functions. Reuses independent Calendar items, existing assignment/publication/capacity rules, workspace and row locks. No existing rows are rewritten. |
| `20260922130000` | Adds scoped communication operation, recipient, coverage and welcome-delivery tables; adds a nullable communication-recipient link to the existing assignment notification ledger. Claims/finalization and unknown provider outcomes preserve prior notification/response/credential records. No implicit campaign send. |
| `20260922140000` | Adds bounded CSV import operations and a batch RPC. Replaces bodies of existing same-signature profile create/update/questionnaire-conversion functions for workspace locking/version checks. Production function-definition anchors needed by these replacements were verified present. Auth users/grants, assignments and response history are outside the import write set. |
| `20260922150000` | Adds workspace photo metadata and volunteer away periods, credential-scoped home/menu functions and away warnings in the bulk preview. Uploads default disabled. No historic object BLOB is assumed to exist. |

Each SQL file is transaction-wrapped and adds forward-only objects or intentional same-signature function replacement. The new functions use explicit grants, SECURITY DEFINER ownership and pinned search paths; new anonymous access consists only of schedule-credential-scoped home/away operations. New tables have RLS. Review found no destructive rewrite of volunteer profiles, private fields, assignments, responses, credentials, notification history, contacts/grants or Calendar data. The currently deployed app may run against the additive migrated schema only after compatibility smoke; the new 12.47 app cannot safely run against the old schema. Plan a maintenance window that suppresses writes between frozen fingerprint, migrations and exact deployment. A partial terminal must fail closed.

Fresh local reset through all four migrations produces 72 functions: 10 anonymous, 47 authenticated-only, 15 internal; PUBLIC and default PUBLIC/anon/authenticated EXECUTE zero; 57 SECURITY DEFINER functions with expected owner/search path. The exact signature set is the previous 58 plus 14 new signatures, with no missing old signature. The original 37 historical `service_role` differences are now traced [signature by signature](12_47_ACL_RECONCILIATION.md): production has a direct `service_role` grant and a public-schema default for that role, while fresh local Supabase lacks that default. Applied migration `20260905130000` deliberately preserved the existing service-role grant. The last **unapplied** 12.47 migration now explicitly grants only those 37 surviving signatures. This is a no-op against the inspected production grants, makes source replay deterministic, leaves the four-migration order unchanged and does not change PUBLIC/anon/authenticated/default ACLs. No historical applied migration was rewritten. This exact source amendment still requires release review before production application.

The expected signature and grant inventory is maintained per function in `scripts/function-privilege-policy.mjs`; the 72-function number is derived from that inventory and independently compared with the isolated catalog, not used to excuse a differing grant. A **second fresh isolated reset** after the exact grant amendment passed `function-privilege-regression.mjs`: 72 exact signatures, 10/47/15 classes, PUBLIC and default PUBLIC/anon/authenticated EXECUTE zero, definer ownership/search paths, future-function denial and direct anonymous mutation denial. Comparison of all 58 old signatures against the read-only production snapshot has **zero** owner/path/grant mismatches. The five focused local regressions for bulk assignment, communications ledger, communications dispatch with a stub provider, volunteer CSV, and volunteer home/photo passed before this narrowly scoped security amendment; no product behavior changed.

The earlier synthetic fixture and synthetic restore remain useful local checks, but the new real-artifact rehearsal supersedes them for Project Local application data. In a `--network none`, no-published-port PostgreSQL container, the backup restored 18 public tables, 347 public rows and 39 migration records at `20260908130000`; nine Auth **ID-only stubs** maintained foreign-key relationships. Its 58 source functions included the expected eight anonymous functions after neutralizing local Supabase defaults before schema replay. The exact four unmodified pending SQL files then applied in order. The local terminal became `20260922150000` (43 records, 26 public tables, 72 functions). Counts and digests of **every original column in all 18 tables** remained identical; the nullable notification-ledger column explains why a whole-row JSON digest changes without original-field data loss. The final catalog passed the exact 10/47/15 policy, no PUBLIC/default PUBLIC/anon/authenticated EXECUTE, expected owners/search paths and RLS on all 26 tables.

## Recovery and photo assets

The backup contract is now **locally amended** for precisely `20260908130000 -> 20260922120000 -> 20260922130000 -> 20260922140000 -> 20260922150000`. The three intermediate terminal locks are recognized for controlled progression while the task stays Disabled; backup execution, task registration and enablement at those intermediates are denied. Each adjacent old-lock/new-DB pair classifies pending; exact matching pairs classify current; wrong/skipped/ahead/future/malformed states fail closed. Focused task fixtures cover interrupted updates and Enabled/Running/unexpected-task states. The prior independent-backup regression continues to cover all historical transitions. This is **local helper proof only**; no live task was started or modified. A migration or verification failure leaves the task Disabled and requires investigation, not an automatic retry or lock advance.

Final local checks after these narrow changes: PowerShell syntax PASS; exact function-privilege regression PASS; 12.47 four-step transition regression PASS; full production-independent backup regression PASS; affected JavaScript ESLint PASS; `git diff --check` PASS. The local Supabase runtime was stopped after verification. TypeScript/build and product browser tests were already passed in the approved 12.47 review and were not repeated for SQL grants, operational scripts and documents.

The historical restore proof is still pinned to 23 migrations, 13 tables and zero product rows, so it must not be used as a current-schema acceptance gate. `scripts/production-backup/Prepare-ProjectLocal1247ScopedRestore.ps1` remains an application-scoped helper. The real backup's `schema.sql` contains **zero Auth and zero Storage table definitions**, while `data.sql` contains 26 Auth and five Storage `COPY` blocks. The first full managed-data import failed on a platform column mismatch. The follow-up [platform recovery proof](12_47_AUTH_STORAGE_RECOVERY.md) instead initialized managed tables with pinned Supabase, imported exact real `auth.users` and `auth.identities` data, intentionally omitted transient Auth state, and verified current empty Storage metadata. No recovery key is missing.

The earlier encrypted application-scoped bundle (144,307 bytes) proved 26-table restore with Auth-ID stubs but did not prove accounts. Its blocked plaintext cleanup was completed by the operator before this task: the prior directory was confirmed absent without inspecting contents. The newer platform-compatible rehearsal imported nine real Auth users and nine identities with source-row hash matches and zero broken Project Local contact/grant links. A new locally encrypted package separates public application/migration data, essential Auth data and current empty Storage metadata. It was independently decrypted into a **second fresh Supabase-initialized target**. All 26 public table counts and row digests matched (347 existing rows), as did Auth hashes, RLS policies, indexes, triggers, function/table ACLs and 222 constraints; one email CHECK differed only in deparsed parenthesis grouping. Exact 72-function ACL/RLS checks and pinned GoTrue/Storage health passed in both targets. A locally signed short-lived token proved restored-user eligibility without sending a real login email. The subsequent synthetic Next.js route smoke is separate evidence; it did not run against recovered production-copy records.

The local hero adapter validates JPEG/PNG/WebP, limits upload size/pixels, strips metadata by WebP re-encoding, creates responsive desktop/mobile derivatives and stores workspace reference/focal points. The local loss/restore regression checks image bytes and hashes, but the current adapter is loopback-only; it rejects Vercel. PostgreSQL backups protect metadata, **not** object BLOBs ([Supabase documentation](https://supabase.com/docs/guides/platform/backups)). A production design is: private primary object bucket; immutable versioned original/desktop/mobile keys; a separate encrypted, operator-owned copy of every published asset and its workspace/asset/version/crop manifest, with SHA-256 per BLOB; daily manifest reconciliation and 30–90 day version retention; restore into fresh isolated storage, verify hashes, then bind matching DB metadata and invalidate cached derivatives. Do not delete a superseded BLOB until retention and a verified independent copy allow it. A small project could use [Cloudflare R2 Standard](https://developers.cloudflare.com/r2/pricing/) for the independent copy: its current free tier includes 10 GB-month of storage and 1 million write-class operations monthly; beyond that, storage is $0.015/GB-month plus operation charges. A few versioned images should remain within that allowance, but actual usage, provider/account, encryption keys and scheduling must be approved and tested. No production bucket, independent object copy or fresh-environment object restore has been proven. **Photo uploads must stay disabled.** A release without photo uploads is technically separable if every database/recovery/security gate passes and the owner separately approves that reduced scope; the default hero remains usable.

## Communications safety

Source and local regressions show new delivery operations link to, rather than recreate, existing assignments and notification rows. Initial versus explicit resend is recorded; per-recipient claims and stable provider idempotency keys prevent uncontrolled concurrent initial sends; unknown outcomes remain pending investigation and are not automatically retried. Schedule credentials and per-day responses remain intact. Welcome eligibility is a pending query, not a send trigger. Assignment creation and CSV import do not call transport. Recipient previews and send authorization use current workspace contacts/grants; each outgoing message has a single recipient and a credential-scoped schedule link. These are local proofs. Before any real campaign, separately authorize one controlled recipient and prove sender, content, link scope, claim/finalization and no duplicate, then explicitly authorize broader sending. Do not infer email enablement from source alone.

## Contact Guide and exact release scope

The owner-supplied illustrated guide remains in `public/guide`. Internal package notes and screenshot-source mapping were moved to `docs/12_47_CONTACT_GUIDE_*` so only the public HTML/CSS/JS and approved fixture images are served. Its current 51 local references and 19 internal anchors resolve. The direct canonical route `/guide` serves the static index through one Next.js rewrite; `/guide/` and `/guide/index.html` redirect to `/guide`. The HTML base path /guide/ keeps relative CSS, JavaScript and images working after the rewrite. The September 24 production-build route smoke passed fresh canonical and redirect loads at 1440px and 390px, all image requests, internal links, console/request errors and horizontal overflow; both captures were inspected. Signed-in desktop and mobile More navigation link to `/guide`; volunteer navigation is unchanged.

A local production build and production-like Next.js preview served `/guide` with HTTP 200, both alternate paths with 308 redirects and the sampled CSS, JS and logo with HTTP 200. Chromium at 1440px and 390px loaded all guide images and verified links, mobile menu/keyboard dismissal, tabs, image lightbox/focus restoration, FAQ, print action, zero console/page/request errors and no horizontal overflow. Captures are in the excluded `previews/12.47-guide` review directory. The guide uses fictional review screenshots, no real email address or credential was found in public source, and it does not claim automatic campaigns or enabled photo uploads. This is a **local** guide proof; the live URL cannot be tested until the exact release is deployed. The proposed per-file release manifest is [12_47_RELEASE_SCOPE.md](12_47_RELEASE_SCOPE.md); its exact file list was reconciled before the local release commit.

## Platform configuration recovery inventory — owner-confirmed custody, untested replacement

The owner inspected provider dashboards on September 24. **Level A (present/configured):** Vercel showed these seven names scoped to Production: `ASSIGNMENT_NOTIFICATION_EMAIL_TRANSPORT`, `ASSIGNMENT_NOTIFICATION_FROM`, `ASSIGNMENT_NOTIFICATION_BASE_URL`, `RESEND_API_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `ADMIN_AUTH_MODE`. Their values were masked and not copied. `RESPONSE_LINK_BASE_URL` is an optional diagnostic/reveal setting, not a required production transport variable. The absence of `SUPABASE_SERVICE_ROLE_KEY` and `ASSIGNMENT_NOTIFICATION_RECORDING_PATH` still requires operator confirmation. The inspected dashboard is not proof of which deployment currently holds the values.

The owner observed Supabase Auth site URL `https://projectlocal.app`, redirects at the canonical and `project-local-one.vercel.app` `/admin/auth/callback` URLs, enabled email provider and secure email change, 3600-second/8-digit email OTP, and custom SMTP from `notifications@projectlocal.app` (Project Local) through `smtp.resend.com` port 465 with username `resend`; password was masked. A publishable and a secret application key are present; the current signing key is ECC P-256 and a legacy HS256 key is recorded as previously used. The owner observed a verified Resend `projectlocal.app` domain and three named API-key records: Supabase Auth (Full access), Production (Sending access), and Onboarding (Sending access). This confirms setting/key-record presence only. The hidden key-to-consumer mapping was not independently established, and this review did not send an OTP or application email.

**Level B (works with deployed app):** the existing app has prior operating history, but this inspection did not prove a fresh Auth/SMTP/Resend delivery, effective environment values in the exact Vercel deployment, or consumer/key association. One specifically approved recipient and separate live proof are required before any campaign. **Level C (securely recreatable):** the logical database and age backup do not include provider account access, hidden API/SMTP/signing values, DNS custody or Vercel deployment configuration. The owner has personally confirmed MFA, account recovery, domain/DNS, credential-reissuance and independent encrypted-backup/age-identity custody arrangements described in [the recovery contract](12_47_AUTH_STORAGE_RECOVERY.md). Masked or nonexportable values should be securely reissued in a disaster, not assumed recoverable as strings. This is an owner attestation, not an independent exercise of provider account recovery or replacement credentials.

Source proves contact login requests an email OTP with callback `/admin/auth/callback` and prevents new Auth-user creation. The database restore proves users and provider identities, but not the hosted Auth settings or future credential custody. The local `supabase/config.toml` is not production evidence. Restored contacts intentionally request a fresh sign-in link after recovery.

The transport expects the reviewed `resend` mode, HTTPS origin and verified sender when enabled. This shell had no Vercel CLI/project link or authorized dashboard session, so the owner-observed variable names/scopes could not be checked independently against the exact deployment; hidden values, key-to-consumer links and recoverability remain unverified. The operator should compare Production, Preview and Development scopes, record only names and state, confirm the two prohibited runtime names above are absent, and retain the owner-confirmed redacted custody checklist. Do not attempt a real OTP or email during this gate.

## Release blockers and optional improvements

1. **Reduced-scope platform recovery locally proven:** the existing real artifact restored with pinned managed schemas, essential Auth user/identity data and current empty Storage metadata; a new encrypted local package independently restored into a second fresh target. Pre-disaster sessions are intentionally not preserved. The exact procedure and exclusions are in [the recovery contract](12_47_AUTH_STORAGE_RECOVERY.md). A separately authorized current-terminal production checkpoint and independent restore remain rollout actions, not facts of this local task.
2. **Local repairs reviewed for the release:** the exact 37-signature grant amendment and four-step lock helper changes pass locally and are included in the exact release scope. Their source is not authorized for live use until a separately approved rollout.
3. **Live configuration/release verification pending:** fresh read-only Vercel email/deployment configuration and exact source-to-schema compatibility smoke must be included in a separately approved rollout.
4. **Photo uploads:** optional for a reduced-scope launch, but production storage, independent encrypted BLOB backup/restore and explicit authorization block enabling uploads.
5. **Local cleanup PASS:** the prior RestoreProof directory and both access-restricted Supabase bootstrap directories were confirmed absent by non-content checks after operator cleanup. The two recovery database volumes, all 22 disposable containers (including the memory-backed decrypted bundle), and all disposable networks were removed. The separate synthetic route-smoke fixture left zero workspace/Auth/volunteer rows; its Next.js and Supabase runtimes were stopped. No recovered production record or expanded encrypted package remains in the test host directories.

The focused route-level smoke used the approved final source in a fresh local synthetic fixture with `ADMIN_AUTH_MODE=enforced`, disabled application email transport and no Resend key. A guest was redirected to login; an authorized synthetic contact loaded Overview, Calendar, Tasks, Volunteers, Needs Attention, Communications and Quick View; a credential-scoped synthetic volunteer loaded home. No browser page/console errors or communication/assignment email-ledger entries appeared. The exact 72-function regression and four-step backup-lock regression passed afterward. This does not replace a browser test against a restored production-copy target, nor the future deployment smoke. Separately held Auth URLs/providers, signing material, email configuration, project keys and deployment environment must still be recreated and verified by their owners in an actual disaster recovery; the logical backup does not contain them.

Optional, nonblocking after the hard gates: stronger historical audit pagination and automated encrypted-manifest verification reports. The approved product implementation should not be redesigned to solve these recovery gaps.
