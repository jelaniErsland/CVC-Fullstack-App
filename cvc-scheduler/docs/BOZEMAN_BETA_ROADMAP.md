# Bozeman Scheduling Beta Roadmap Re-baseline

This document supersedes the previous mechanical post-12.13 next-step assumption for near-term work. It does not erase historical sequencing; it re-baselines the current repository against the new target: a small, production-safe Project Local beta for real Bozeman volunteer scheduling, ideally ready by mid-August 2026.

The governing principle is: **Cut features, not integrity.**

Belgrade remains on the existing Google Sheets/App Script workflow and is the operational fallback. Do not migrate active Belgrade operations as part of the Bozeman beta milestone.

## Concise current-state summary

- Workspace, project-contact grant, questionnaire submission, volunteer profile, task preset, Calendar item, Calendar assignment/current response, public response-token, response route, and assignment-detail foundations exist.
- `/admin/calendar` is already cut over to persisted Calendar item reads and now supports the first narrow persisted one-off timed item create/edit path. It is server-owned, dynamic/no-store, bounded by server-derived Day/Week/Month/List ranges, workspace/contact/capability scoped, and free of mock/persisted item mixing.
- The Calendar read states remain ready with items, ready empty, unavailable, and error; the beta roadmap does not change those state semantics.
- `/admin/volunteers` is cut over to persisted volunteer-profile truth for the narrow manual Add/Edit path, `/admin/tasks` is cut over to persisted task-preset read/create/archive truth, and `/admin/needs-attention` now derives its core staffing/response inbox from persisted Calendar/assignment/current-response truth. `/admin/announcements`, the historical Needs Attention detail route, and the public `/v/demo` volunteer portal remain mock/prototype surfaces.
- `lib/tasks/readModelContract.server.ts` now records the route-used 12.37 persisted Tasks contract: effective `tasks.view` reads, `tasks.edit` create/archive, no general edit, no mock fallback, and no Calendar-occurrence decoration.
- Calendar task-preset selection, the first Calendar volunteer assignment picker/create/cancel path, the draft/private versus published/live Calendar visibility boundary, secure account-light volunteer schedule access, volunteer Confirm/Deny from `/v/schedule`, and initial assignment notification email delivery are implemented and hosted-validated through 12.22.1. Communications persistence, public lookup, remembered devices, and response-link admin reveal/copy activation remain unimplemented or intentionally paused.
- The approved visual direction is represented by the permanent references in [`docs/design/approved-project-local-ui`](./design/approved-project-local-ui/); beta-critical surfaces must launch with that polished Project Local direction, not a utilitarian developer/admin interface.

## Bozeman Beta launch gate

The Bozeman beta may launch only when the narrow scheduling loop is production-safe end to end:

1. A real Bozeman workspace exists and authorized project contacts/helpers have correct capability-scoped access.
2. Jelani and trusted helpers can add or import permanent volunteer profiles.
3. Authorized schedulers can create and edit real Calendar scheduled items.
4. Scheduled items can be created from reusable task presets or first-class one-off custom definitions.
5. Volunteers can be assigned to scheduled items.
6. Draft/private versus published/live visibility is correct and does not depend on email success.
7. Assigned volunteers can securely access their own schedules without normal user accounts.
8. Volunteers can Confirm or Deny assignments.
9. Authorized project contacts can see accurate response state from assignment/current-response truth.
10. Basic initial assignment notification email can be sent reliably.
11. Beta-critical admin and volunteer surfaces use the approved Project Local UI direction.
12. Production Supabase, deployment, auth, email provider, domain/base URL, hosted validation, observability, backup/recovery, mobile/browser proof, and rollback/fallback planning are complete.

Belgrade Sheets/App Script remains the fallback if this gate is not safely met.

## Dependency-based critical path

1. `12.14 Bozeman Workspace Access and Provisioning Readiness`
   - Completed as the permanent operator provisioning boundary for workspaces, approved Auth identities, project contacts, and explicit grants. It does not create real Bozeman data automatically.
   - Unblocks: the first real Bozeman workspace, contact identities, grants, and all later beta admin access.
2. `12.15 Manual Volunteer Profile Add/Edit Permanent Path`
   - Completed as the permanent persisted `/admin/volunteers` manual Add/Edit path. It unblocks the first real Bozeman volunteer record and the future assignment picker source.
3. `12.15.1 Hosted Staging Migration + Volunteer Profile Management Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). Staging advanced from `20260705000000` to `20260714121500`, and the hosted gate passed migration/RPC/provenance, generated-type, RLS/capability, isolation, direct-table-denial, safe-error, and zero-residue checks.
4. `12.16 Calendar Create/Edit Scheduled Item Implementation`
   - Completed as the first narrow persisted Calendar item create/edit path: one-off timed items only, defaulting Follow-up Contact to the authenticated scheduler.
5. `12.16.1 Hosted Staging Calendar Item Management Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). Staging advanced from `20260714121500` to `20260714121600`, and the hosted gate passed generated-type parity, one-off timed create/edit, Follow-up Contact, zero-needed/read-model, RLS/capability, isolation, direct-table-denial, malformed/source-validation, existing-row compatibility, safe-output, and zero-residue checks.
6. `12.17 Calendar Task Preset Selector and One-Off Definition Path`
   - Completed locally and hosted-validated through 12.17.1: preset-derived and one-off scheduled item creation without requiring the full `/admin/tasks` cutover first.
7. `12.18 Volunteer Assignment Picker and Create/Cancel Commands`
   - Completed locally and hosted-validated through 12.18.1: `/admin/calendar` can assign ready persisted volunteers and cancel active assignments through reviewed persisted boundaries.
8. `12.18.1 Hosted Staging Assignment Management Validation Gate`
   - Completed: non-production staging is validated at `20260714121800` with generated type parity, picker projection, atomic create, cancellation, coverage truth, direct-write-denial, isolation, and zero-residue proof.
9. `12.19 Draft/Private Versus Published/Live Calendar Visibility`
   - Completed locally: new Calendar items are private drafts owned by the creating project contact, published items become visible to authorized same-workspace contacts, and publication remains separate from email/volunteer schedule access/response-link activation.
10. `12.19.1 Hosted Staging Calendar Publication Visibility Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). Staging advanced from `20260714121800` to `20260714121900` during the gate after a safe unapplied migration comment typo fix, and the final hosted validation passed generated-type parity, publication defaults, draft/published read privacy, publish authorization, assignment/token/public-response gating, direct-table-denial, isolation, safe-output, and zero-residue checks.
11. `12.20 Secure Account-Light Volunteer Schedule Access`
   - Completed locally and hosted-validated through 12.20.1: authorized schedulers can issue/revoke dedicated hash-only volunteer schedule access tokens, `/v/access/[token]` exchanges the bearer for a session-only HttpOnly cookie, and `/v/schedule` reads only that volunteer's published assignments.
12. `12.20.1 Hosted Staging Volunteer Schedule Access Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). Staging was verified at `20260714122000` before and after the successful run; the hosted gate passed generated-type parity after a UTF-8-safe refresh from hosted output, dedicated token separation, hash-only storage, TTL policy, issuance/revocation authorization, public ready/empty/unavailable reads, schedule filtering, response truth, safe projection, direct-table denial, isolation, multiple credentials, last-used behavior, hosted route exchange, clean URL, cookie attributes, host preservation, Not-you clearing, mobile layout, accessibility focus, safe output, and exact-run plus namespace zero residue.
13. `12.21 Volunteer Confirm/Deny Round Trip`
   - Completed locally and hosted-validated through 12.21.1: account-light volunteers can Confirm, mark `Can’t make it` with optional notes, reverse eligible responses, and Confirm All through persisted assignment/current-response truth.
14. `12.21.1 Hosted Staging Volunteer Response Round-Trip Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). Staging was verified at migration `20260714122100` before and after the successful run; the hosted gate passed generated-type parity, Confirm/Deny, denial notes, Confirm All, 48-hour/start locks, `volunteer_schedule` versus `public_token` provenance separation, admin Calendar response truth, direct-write denial, hosted route/cookie/mobile proof, safe output, and exact-run plus namespace zero residue.
15. `12.22 Initial Assignment Notification Email Boundary`
   - Completed locally: `/admin/calendar` exposes an explicit scheduler-controlled initial assignment email action for published items with active assignments, eligible volunteers, valid recipient email, Follow-up Contact volunteer-facing data, duplicate-send prevention, delivery ledger state, schedule-access token issuance, and a disabled-by-default server-only transport.
16. `12.22.1 Hosted Staging Initial Assignment Notification Validation Gate`
   - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). The gate preserved hosted history with forward validation fixes through `20260714122230`, refreshed generated public-schema type parity, validated explicit send summary/claim/finalize behavior, malformed recipient/finalize bounds, duplicate/concurrency/retry/stale recovery, hosted browser send/retry behavior with recording-only transport, schedule-access secrecy, no response-link activation, direct-table denial, safe output, and exact-run plus namespace zero residue.
17. `12.23 Bozeman Beta UI Polish, Hosted Validation, and Launch Gate`
    - Completed as a launch-candidate gate, not a production launch. Focused Calendar/Volunteers/volunteer-schedule polish was applied, `lib/readiness/bozemanBetaLaunchGate.server.ts` records a server-only route-unused launch gate, `docs/BOZEMAN_BETA_LAUNCH_RUNBOOK.md` and `docs/BOZEMAN_BETA_GO_NO_GO.md` record operations and decision evidence, and the conclusion is `NO-GO` until production provider/domain/deployment/observability/backup/operator/pilot actions are completed.
18. `12.23.1 Hosted End-to-End Bozeman Beta Loop Integration Gate`
    - Completed against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`) with the target `ACTIVE_HEALTHY` at migration `20260714122230` before and after. One disposable hosted namespace exercised the real product routes continuously across Auth/session, persisted Volunteer Add/Edit, Calendar one-off and preset-backed scheduling, assignment, draft privacy, publication, recording-only Initial email, secure volunteer schedule handoff, Confirm, `Can’t make it`, Confirm All, admin response/coverage truth, view-only/wrong-workspace denial, missing-prerequisite handling, inside-48-hour and started-state behavior, desktop and 390px schedule proof, safe-output checks, generated-type parity, and zero hosted residue. It added no product feature, migration, generated-type change, production access, real email, service-role application path, response-link reveal/copy, public lookup, remembered devices, real Bozeman data, or Belgrade migration.
19. `12.24 Production Environment, Deployment, Domain, and Auth Readiness`
    - Completed as a production-readiness/operator-handoff slice only. It recommends Vercel based on the current Next.js repository shape; adds a server-only, route-unused production readiness contract; adds production environment inventory, deployment runbook, and Jelani setup checklist; and keeps the launch conclusion `NO-GO`. No production Supabase project, production deployment, DNS change, real Bozeman data, real email provider, production migration, service-role application path, response-link reveal/copy, or deferred feature expansion was added.
20. `12.25 Production Supabase Migration, Type-Parity, and Read-Only Validation Gate`
    - Completed: the exact initial/bootstrap empty-production gate was run against `project-local-production` (`wdlaauzknfggoqldolmx`). The first run correctly refused while the local CLI was still linked to staging, then the approved production ref was linked, the dry-run showed only reviewed committed migrations through `20260714122230`, production advanced from a clean initial migration state to `20260714122230`, generated public-schema type parity passed, product/Auth/storage counts remained zero before Auth setup, public Supabase connectivity passed, structural RLS/security checks passed, staging was not targeted, and the production CLI link was removed afterward. Preserve this historical bootstrap evidence; 12.36 supplies the separate established-production gate for later live-state migrations.
21. `12.26 Production Deployment, Auth, and Read-Only Smoke Validation`
    - Completed as deployment/Auth evidence and smoke-gate preparation, not launch approval. Vercel project `project-local` is live and Ready at `https://project-local-one.vercel.app`; production Auth is enforced; production Supabase public values are configured in Vercel Production only; service-role/email transports are absent; Supabase Auth Site URL and callback are configured for the temporary origin; manual magic-link sign-in passed; signed-in no-grant admin routes failed closed; invalid volunteer access remained unavailable; and the new exact-opt-in public smoke gate is ready to validate anonymous landing/admin/login/volunteer unavailable behavior without mutation after this checkpoint is committed. The same public HTTP assertions passed during implementation through a separate non-mutating diagnostic.
22. `12.27 Final Production Domain and Auth Validation`
    - Completed as final-domain evidence and public smoke validation. Canonical production origin `https://projectlocal.app` is manually configured and working; Vercel project `project-local` remains live with temporary fallback alias `https://project-local-one.vercel.app`; production Auth remains enforced; Supabase Auth Site URL and exact final-domain callback are configured; the temporary Vercel callback remains allowlisted for fallback; manual magic-link sign-in returned through the final domain and opened the admin shell; no-workspace/no-grant Calendar and Volunteers routes still failed closed; no Project Local product row was created; commit `082c960` was pushed to `origin/master`, the Vercel Production deployment sourced from `082c960` reached Ready, and the exact final-domain production smoke passed before push and after deployment with exit code `0`.
23. `12.28 Production Backup, Recovery, and Rollback Readiness`
    - Completed as a route-unused recovery-readiness/documentation slice only. `lib/readiness/productionRecoveryReadiness.server.ts`, `npm run test:production-recovery-readiness`, and [`PRODUCTION_BACKUP_RECOVERY_RUNBOOK.md`](./PRODUCTION_BACKUP_RECOVERY_RUNBOOK.md) document application rollback through Vercel, migration-forward database recovery, grant-revocation/email-disable operational pause, safe recovery verification, and Belgrade fallback. 12.28.1 records operator evidence that production Supabase is on Free, Supabase-managed scheduled backups and restore-to-new-project are unavailable on that plan, no first backup timestamp/status or retention exists yet, PITR is unavailable and intentionally not required for the initial beta, independent encrypted backups are the preferred current plan, Supabase Pro remains optional, and restore owner/test plus incident owners remain launch blockers.
24. `12.29 Independent Encrypted Production Backup Automation Foundation`
    - Completed as a Windows-first operator-owned automation foundation only. Added PowerShell scripts for DPAPI secret setup, guarded Supabase CLI logical dumps, age encryption, private OneDrive encrypted storage, bounded retention, credential-free status, explicit Task Scheduler registration, and local/disposable restore-drill guardrails, plus [`INDEPENDENT_PRODUCTION_BACKUP_SETUP.md`](./INDEPENDENT_PRODUCTION_BACKUP_SETUP.md) and `npm run test:production-independent-backup`. No production backup, restore, task registration, credential, age identity, production access, data, migration, generated-type change, GitHub Actions artifact, or runtime/product behavior changed.
25. `12.30 First Beta UI Visual Integration Attempt`
    - Functionally validated but rejected in product-owner visual review. Persisted Calendar, Volunteers, and volunteer-schedule browser coverage passed, but the presentation remained materially inconsistent with the approved Project Local concepts and too close to the prior prototype. 12.30 is not an approved design baseline.
26. `12.30.1 Beta UI Visual Direction Correction`
    - Completed and explicitly product-owner approved by Jelani. The permanent reference contract lives in [`design/approved-project-local-ui`](./design/approved-project-local-ui/); the shared shell, persisted Calendar and Volunteers routes, and secure volunteer schedule use its brand, hierarchy, density, navigation, surfaces, calendar-workspace, and inspector language while preserving all persisted commands, capability boundaries, cookie/token rules, and fail-closed states. Jelani reviewed and accepted all six real-route desktop/390px captures in [`previews/beta-review`](./previews/beta-review/). Screenshot writing remains opt-in through `WRITE_BETA_REVIEW_SCREENSHOTS=1`, and human-readable values remain limited to disposable review fixtures. The beta-critical UI gate is proven and no longer blocking. No architecture, schema, RPC, feature, production data, hosted target, external service, pilot approval, or production launch approval changed.
27. `12.31 Production Transactional Email Provider Integration`
    - Completed as application integration only. Resend is the selected initial production provider and now sits behind the existing 12.22 server-only email boundary alongside disabled and recording transports. Explicit fail-closed environment validation, concise Project Local text/HTML, non-secret deterministic provider idempotency, bounded message-id handling, safe provider/network failure normalization, and credential-free claim/provider/finalization outcome signals are covered by fake-network regression. The database ledger remains authoritative for duplicates, retries, and stale recovery. No real provider request, email, account/key/domain, DNS/Vercel setting, hosted database, migration/RPC/RLS/Auth/token/cookie behavior, or approved UI changed. Resend operator configuration, real deliverability proof, monitoring, backup/restore, observability, real provisioning, and controlled pilot evidence remain blocking; launch remains `NO-GO`.
28. `12.31.1 Resend Operator Configuration and Provider Deliverability Evidence`
    - Recorded August 10, 2026 operator evidence only. `projectlocal.app` is verified and ready in Resend; `Project Local <notifications@projectlocal.app>` is the verified sender; a restricted domain-scoped Sending key is stored only in encrypted Vercel Production settings; the canonical base URL and sender are configured; open/click tracking are off; and a direct Resend-dashboard message arrived in an approved Gmail inbox. This proves provider/domain/sender/basic inbox delivery only. No Project Local Initial email or production ledger row was created. The application transport was removed after a temporary no-send enablement, the current deployment is Ready/Latest, and Project Local application email is disabled. Application-ledger delivery, duplicate/schedule-link/retry proof, email-failure monitoring, backup/restore, provisioning, and pilot evidence remain blocking; launch remains `NO-GO`.
29. `12.32 Production Observability Foundation`
    - Completed as the privacy-safe application layer only: bounded server events cover representative beta-critical failures and finalized assignment-email success, logging cannot change product behavior, and a route-unused injected stale-`sending` detector neither queries nor mutates production. No external sink or hosted operation was added. Operator production evidence remained a separate gate.
30. `12.32.1 Production Operator Observability Evidence`
    - Records August 11, 2026 proof that 12.32 events reach searchable/filterable Vercel Production Logs, deployment/build status is reviewable in Vercel Deployments, the Project Local product/operator owner owns alerts and incident response, an action policy is documented, and a controlled invalid-token request produced a privacy-safe `schedule_access.exchange_failure` warning without data creation or email. The authorized stale-delivery production read path, cadence, threshold/escalation proof, and practical notification mechanism beyond manual Vercel Hobby review remain blocking; launch remains `NO-GO`.
31. `12.33 Authenticated Assignment Notification Health`
    - Proves the authenticated capability-gated Notification Health architecture, local/staging behavior through `20260811123300`, and the sufficient manual cadence/escalation policy for the initial tiny beta. Production execution remains pending and application email remains disabled.
32. `12.34 Independent Production Backup Execution and Local Restore Proof`
    - Proves one read-only production backup using the unchanged six-file package, age encryption before persistence, `2026-08-12T17:26:46.3144615Z` status, `62409` encrypted bytes, matching SHA-256, retention recognition, and plaintext cleanup. The disposable loopback restore stopped at `roles.sql`; a synthetic local role-only dump reproduced the managed-role privilege failure while all required platform roles were present. Full restore/post-restore proof remains blocking and launch remains `NO-GO`.
33. `12.34.1 Supabase Managed-Role Restore Compatibility and Full Local Restore Proof`
    - Solves the managed-role replay boundary without changing the six-file package: the actual eight-statement roles dump is fully classified, four represented Supabase-managed roles/settings/privilege are verified rather than recreated, supported synthetic user-role material remains applicable, and unsupported SQL fails closed. One fresh loopback restore passed roles, schema, 23-version migration history through `20260714122230`, data, baseline functions, pending-function absence, RLS, and FORCE RLS, then stopped on 26 unsafe direct `TRUNCATE` grants to `anon`/`authenticated`. Remaining type/product/application checks were not reached. Full recovery remains blocking and launch remains `NO-GO`.
34. `12.34.2 Restored Grant Attribution`
    - Attributes the 26 direct `TRUNCATE` grants to `RESTORE_INTERACTION`, not historical source ACL truth, without weakening or changing the six-file package.
35. `12.34.3 Deterministic Source ACL Recovery and Hosted Staging Privilege Hardening`
    - Reconstructs the exact historical source ACL, completes local recovery-forward through `20260812123430`, and proves generated-type, schema/migration, RLS/FORCE RLS, exact direct/default privilege, product-row, Notification Health, Calendar/assignment, Volunteer, and volunteer-schedule compatibility with zero residue. The privilege-hardening migration is validated on approved staging through `20260812123430`. Full independent technical recovery is proven; historical production state is superseded by 12.36.5.
36. `12.35 Windows Recurring Backup Operations`
    - Registers the enabled current-operator daily `03:15` task with `StartWhenAvailable`, proves credential-free atomic failure status and a human-confirmed Windows notification through a scheduled pre-network fixture, cleans its temporary task/status, and records Jelani/the Project Local product-operator owner for backup response, restore, Vercel rollback, operational pause, email disablement, and pilot cancellation. The one authorized scheduled production execution failed safely at `migration_preflight_failed` with no new encrypted artifact, so successful scheduled checksum/retention execution remains blocking. Production remains recorded at `20260714122230`; application email remains disabled and launch remains `NO-GO`.
37. `12.35.11 Controlled One-Shot Scheduled Production Backup Proof`
    - Proves exactly one scheduled-host production execution through read-only migration preflight, all five credential-safe native dump stages, the unchanged six-file package, age encryption, atomic daily/weekly publication, matching SHA-256, Sunday promotion, retention, duplicate prevention, and zero plaintext residue. The temporary task is removed; the permanent task does not run.
38. `12.35.12 Backup/Recovery Evidence Reconciliation and Recurring Task Decision`
    - Maps every recovery requirement to durable evidence, confirms local independent-backup/recovery suites, proves with a harmless missed-trigger clone that enabling does not catch up on this host, and enables the unchanged permanent daily `03:15` task without execution. Backup/recovery is complete and non-blocking. Application-driven Initial email, production migrations/first Notification Health execution, real Bozeman provisioning, controlled pilot, and explicit launch approval remain blocking; launch remains `NO-GO`.
39. `12.36.5 Controlled Established-Production Migration Application`
    - Completed the authorized production transition through `20260811123300` and `20260812123430`, with postflight preservation of the approved Auth identity and empty Project Local product state. The permanent backup task is enabled/Ready at lock `20260812123430`; the schema blocker is removed. Application email, first real authorized Notification Health execution, real Bozeman provisioning, and controlled pilot remain blocking; launch remains `NO-GO`.
40. `12.36 Established-Production Migration Gate Readiness`
    - Preserves the historical 12.25 bootstrap gate and adds a separate established-production preflight/apply/postflight contract. The local disposable transition from exactly `20260714122230` through exactly `20260811123300` and `20260812123430` preserves synthetic Auth/product state and proves exact history, generated types, Notification Health, direct/default privileges, RLS/FORCE RLS, platform ownership, future-table defaults, refusal paths, and zero residue. The future backup-task lock transition is locally proven, but the live enabled/Ready task remains unchanged at `20260714122230`. Production/staging were not contacted; the production application and first health execution remain blocking, and launch remains `NO-GO`.
40. `12.37 Persisted Tasks Page Cutover and Reusable Task Creation`
    - Replaces `/admin/tasks` mock truth with a polished persisted task-preset library using server-derived exactly-one-workspace context, effective `tasks.view`, and existing `tasks.edit` create/archive RPCs. The route has no mock fallback, read-only users keep a useful library, system presets remain protected, and Calendar's active selector sees created presets but excludes archived ones while preserving existing occurrence snapshots. General persisted edit remains deferred. This local-only slice adds no migration, hosted access, backup change, email, or real Bozeman data; launch remains `NO-GO`.
41. `12.38 Persisted Overview Cutover and Real Project Home`
    - Replaces `/admin/dashboard` mock truth with a dynamic/no-store persisted project home using effective `workspace.read` and deterministic server-owned workspace scope. Existing reviewed Calendar, Tasks, and Volunteers readers provide only capability-authorized bounded projections; upcoming work, derived coverage/response review signals, lightweight summaries, authorized actions, and one calm ready-empty state replace prototype rows. No Needs Attention persistence, new database query/RPC/table/migration/type, service-role path, hosted access, email, backup change, or real Bozeman data is added. Jelani approved the five local real-route captures and 12.38.1 committed the reviewed cutover; launch remains `NO-GO`.
42. `12.39 Persisted Needs Attention Staffing and Response Inbox`
    - Replaces `/admin/needs-attention` mock index truth with a server-owned dynamic/no-store action inbox derived from the existing Calendar/assignment/current-response projection. Effective `workspace.read`, `calendar.view`, and `assignments.view` plus exactly one active workspace are required; edit is not. Published underfilled work appears within 14 days, pending/denied responses within 21 days, urgency uses trusted workspace time, and rows expand to safe current coverage plus a Calendar context action. Draft/passed/canceled/outside-window truth is excluded. Needs Attention is promoted in desktop/mobile primary navigation and Volunteers remains in mobile More. No issue persistence, dismiss/resolve/history workflow, availability/conflict logic, migration, SQL/RPC/type change, hosted access, email, backup change, or real Bozeman data is added. Six local captures await product-owner review; launch remains `NO-GO`.
43. `12.40 Persisted Assignment Detail Product Entry and Admin Drill-Down`
    - Makes the existing persisted assignment-detail route reachable from exact Calendar roster assignments and exact Needs Attention pending/denied assignments, including separate links when one signal contains multiple assignments. The route preserves its reviewed authenticated `assignments.view` reader, workspace isolation, minimal projection, and unavailable state while adopting the approved Project Local admin shell on desktop/mobile. Response-link reveal/copy and token issuance remain disabled; there is no response/edit mutation, broad assignment directory/search, Volunteer-page entry, migration/RPC/type change, hosted access, email, backup change, service-role path, or real Bozeman data. Local review captures await product-owner review; launch remains `NO-GO`.
44. `12.41 First Real Bozeman Production Workspace and Admin Provisioning`
    - Uses the reviewed 12.14 operator boundary to create exactly one active `bozeman` workspace, one active project contact for the single approved Auth identity, and one active canonical `mainScheduler` grant. Authenticated RLS resolves exactly that one workspace/contact/grant; normal Auth resolves the approved contact into the real `Bozeman` workspace and persisted empty Overview. Every other product/storage count remains zero. Production stays at `20260812123430`, application email stays disabled, Notification Health is not invoked, and launch remains `NO-GO`. Authenticated admin navigation is observed at approximately 2–5 seconds and requires investigation before controlled-pilot expansion; no cause or remediation is claimed.
45. `12.42 Authenticated Admin Navigation Performance Investigation`
    - Establishes the code-level latency chain without changing product behavior: proxy Auth verification plus a page-local client and three serial page `auth.getUser()` checks around grant/contact/workspace resolution. Tasks and Volunteers each add one persisted read; Overview parallelizes its authorized Calendar/Tasks/Volunteers summaries after the same context chain; Needs Attention adds the bounded 21-day Calendar model; and Calendar adds a bounded core model, selector, picker, and notification-summary reads serially. Empty Calendar has ten network round trips; populated Calendar can reach sixteen across fifteen sequential stages, including duplicated task-preset and assignment/response reads. Communications is the fast mock/in-memory control. A safe request-scoped context and Calendar query orchestration pass are deferred to 12.42.1; no caching, authorization change, migration, hosted configuration, or production deployment is included. Workspace/project switching is separately queued as a secondary Settings UX concern; prototype `/admin/projects` remains non-production workspace truth.
46. `12.42.1 Request-Scoped Verified Admin Context Performance Fix`
    - Reuses one explicit server-only verified context across persisted Dashboard, Tasks, Volunteers, Needs Attention, and Calendar page reads while preserving proxy Auth, one fresh page `auth.getUser()`, the same cookie-bound client/RLS session, effective grant/contact/workspace lifecycle checks, exactly-one-workspace route selectors, and independent mutation authorization. The three existing narrow context DB reads now start concurrently after Auth; expected total calls including proxy fall from 8 to 6 for empty Tasks/Volunteers/Needs Attention and from 10 to 8 for empty Overview/Calendar. Calendar-specific readers remain serialized and duplicated data families remain for later work. Deterministic local call-graph and isolation proof passes; there is no cache, schema/RPC/RLS/type/service-role/hosted/UI/workspace-switching change and no production speed claim before owner testing after deployment.
47. `12.42.2 Autonomous Admin Navigation Performance Debugging and Targeted Fix`
    - Records that deployed 12.42.1 did not produce an owner-observed speed improvement and adds a loopback-only disposable-fixture harness for actual route readers, call graphs, concurrency, and deterministic 100/250/400 ms synthetic remote latency. A single authenticated context RPC is rejected because it lowers calls but not the four-stage current shared critical path. The source-only Calendar correction parallelizes its unchanged authorized core/selector pair and, after item ids exist, its unchanged picker/notification pair. Empty Calendar remains eight calls and improves from six to five critical stages; the populated model remains fourteen calls and improves from eleven to nine. Existing RLS, lifecycle/effective-grant/capability/exactly-one-workspace behavior, bounded reads, Assignment Detail, and fresh mutation authorization remain intact. Duplicate Calendar task/assignment/response families remain for later review. No schema/migration/RPC/RLS/type/privilege/service-role/hosted/email/backup/workspace-switching change or production speed claim is included; the uncommitted fix requires review and deployed owner verification.
48. `12.42.4 Admin Navigation Feedback and Performance Wrap-Up`
    - Records owner/operator evidence that production Supabase is in Oregon (`us-west-2`) while Vercel Functions were observed in `iad1`; Vercel Function Region was manually changed to Portland (`pdx1`), the same reviewed application was redeployed, and product-owner verification reports dramatically snappier authenticated admin navigation. The small source-only follow-up adds a decorative shell-preserving Next Link pending indicator for existing desktop/mobile navigation. 12.42.2 Calendar orchestration remains a valid secondary improvement. General navigation performance is no longer the primary pre-pilot blocker, although no exact latency or universal route claim is made. No infrastructure, Auth, RLS, capability, read/write, schema/migration/RPC/type, email, Notification Health, backup, or workspace-switching change is included.

## Repository-grounded beta blockers

- Real Bozeman workspace provisioning is complete: 12.41 used the reviewed operator boundary for exactly one workspace/contact/grant and proved exactly-one-workspace authenticated resolution.
- Controlled volunteer import does not exist; manual persisted volunteer Add/Edit now exists through `/admin/volunteers`.
- `/admin/calendar` has a narrow one-off timed create/edit path, and its 12.16 migration/RPC/type changes have passed the required hosted staging validation gate.
- 12.17 is implemented and hosted-validated as the Calendar task-preset selector and one-off definition path: `/admin/calendar` now reads active persisted `task_presets` for the resolved workspace when the same contact has `tasks.view`, can create preset-backed timed scheduled items through the existing create RPC, keeps custom one-off timed creation, and can edit preset-backed timed occurrences through a new allowlisted RPC without changing their source. 12.17.1 completed hosted staging validation on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`) through migration `20260714121700`.
- Draft/private versus published/live visibility is implemented and hosted-validated through 12.19.1. Staging is validated at `20260714121900`; draft/private reads, published/live reads, publication authorization, assignment preparation privacy, response-token/public-response gating, generated-type parity, direct-write denial, and zero-residue cleanup passed on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`).
- 12.18 and 12.18.1 are complete. The first Calendar volunteer assignment picker/create/cancel path uses persisted volunteer profiles and assignment/current-response truth, and the `20260714121800` migration/RPC/generated-type boundary has passed hosted non-production validation before hosted beta use.
- Secure account-light volunteer schedule access and volunteer Confirm/Deny are implemented and hosted-validated through 12.21.1. `/v/demo` remains mock and separate. `/v/schedule` persists Confirm/Deny/Confirm All through credential-scoped public RPCs, and `/respond/[token]` shares the same 48-hour/start-lock mutation policy while retaining `public_token` provenance.
- Confirm/Deny no longer depends on mock volunteer state locally. It persists to the existing one-current-row `assignment_responses` truth and is visible to authorized admins through existing Calendar assignment-derived response state.
- Basic initial assignment email delivery has a hosted-validated 12.22/12.22.1 ledger boundary and a 12.31 Resend application adapter: explicit admin send only, server-derived recipient and Follow-up Contact eligibility, duplicate-send prevention, hash-only schedule-access credential issuance, safe delivery metadata, deterministic provider idempotency, and disabled/recording/resend transports. August 10 operator evidence proves Resend domain/sender/key/Vercel configuration, disabled tracking, and direct provider-level Gmail delivery only. Project Local application-driven production delivery through that ledger, duplicate/schedule-link/retry proof with real Resend, and email-failure monitoring remain beta blockers; application transport is currently disabled.
- 12.30 was functionally validated but rejected visually. Jelani explicitly product-owner approved 12.30.1 on the real Calendar inspector/assignment/email surfaces, persisted Volunteers Add/Edit, secure volunteer schedule response states, and shared admin shell, and accepted all six desktop/390px review captures. The beta-critical UI gate is proven and no longer a launch blocker. The controlled pilot remains a separate blocking gate.
- Production Supabase baseline schema, final-domain/Auth evidence, final-domain production smoke, the 12.36.5 established-production migration, and exact 12.41 Bozeman workspace/contact/grant provisioning are complete. Approved staging and production are at `20260812123430`, and the permanent task is enabled/Ready at that lock. Backup/recovery is complete and non-blocking. Owner/operator evidence establishes that the prior systemic navigation delay was the Vercel `iad1` to Supabase Oregon region mismatch: the reviewed application was redeployed after the manual Vercel Function Region change to `pdx1`, and the owner reports dramatically snappier authenticated admin navigation. 12.42.2 Calendar orchestration remains a secondary validated improvement. General navigation performance is no longer the primary pre-pilot blocker; a specifically problematic route still requires evidence. First real authorized Notification Health execution, application-driven email proof, and pilot gates remain incomplete. Supabase Pro remains optional. The current conclusion is `NO-GO`.

## Non-blocking features deferred behind the beta gate

- Belgrade migration from Sheets.
- General persisted task-preset edit and advanced custom-field authoring.
- Full public questionnaire workflow.
- Manual public volunteer lookup.
- Remembered-device behavior.
- Assignment-detail response-link admin reveal/copy activation.
- Full Communications authoring, automatic reminder cadence, announcement blasts, delivery analytics.
- Advanced availability/conflict engine.
- Drag/drop, resize, recurrence, copy/paste Calendar persistence.
- Food and Security as separate restored modules.
- Persisted Needs Attention dismissal, accepted-exception, resolution, and history workflow.
- Broad assignment directory/search.

## Decision on old 12.14

Old recommendation: `12.14 Route-Unused Persisted Tasks Read Model Helper / Query-Shape Review`.

Decision: move and modify it. A full `/admin/tasks` read helper remains useful, but it is not the highest-priority Bozeman beta unblocker. Calendar writes can safely proceed with a narrower reviewed task-preset selector/read seam, while the immediate critical path starts with Bozeman workspace access and permanent volunteer data entry.

Immediate next recommended slice: controlled application-driven Initial Assignment Email proof and first real production Notification Health check.

Return to the Tasks helper as either:

- a scoped Calendar task-preset selector seam before or during Calendar create/edit work; or
- the full `/admin/tasks` helper after beta-critical scheduling is safe.

## Safest shortest path to first real-world moments

- First real Bozeman volunteer record: with the 12.41 workspace/contact/grant foundation already provisioned, create one approved volunteer through the persisted `/admin/volunteers` manual Add/Edit path.
- First real persisted scheduled item created from product UI: keep the current persisted `/admin/calendar` read route, add a reviewed `calendar.edit` server action for a narrow create/edit path, then refresh the persisted route.
- First real volunteer assignment: add a volunteer picker read seam and `assignments.edit` create command for one active volunteer/item pair with duplicate prevention.
- First published volunteer-visible assignment: define publication visibility truth before email; visibility must not depend on delivery success.
- First real Confirm/Deny round trip: connect secure account-light volunteer schedule or token route to current `assignment_responses` without reopening unrelated assignment-detail response-link copy UI.
- First real assignment notification email: send a basic initial assignment email with resolved recipient, secure schedule/action link, duplicate-send prevention, safe failure observability, and no broad Communications feature expansion.

## Validation gates before beta launch

- Static regressions for every new server boundary and route integration.
- Local disposable validation for volunteer, Calendar write, assignment, public response, and email boundaries where useful.
- Hosted validation for any migration, RPC, generated type, hosted script, or hosted behavior change.
- Desktop and 390px mobile browser validation for beta-critical admin and volunteer routes.
- No raw Supabase/status/start output in transcripts; diagnostics remain redirected/redacted.
- No mock/persisted truth mixing.
- No unsafe field leakage: credentials, tokens, bearer/verifier, raw grants/capabilities, SQL/RPC detail, provider dumps, stack traces, questionnaire/emergency contact details, unrelated rows.
- Production environment review for Supabase, deployment, auth, domain/base URL, email provider, secrets, logging, backups, and recovery.
- Pilot test with disposable or approved Bozeman data.
- Explicit rollback/fallback plan with Belgrade remaining on Sheets.

## Approved UI integration sequence

1. Preserve the current Calendar shell while adding polished create/edit inspector behavior.
2. Integrate the volunteer picker and manual Add/Edit Volunteer surfaces.
3. Integrate the volunteer schedule home, assignment detail, and response states.
4. Integrate the initial email review/send/status surfaces.
5. Normalize empty/loading/unavailable/error states across beta-critical routes.
6. Run a focused beta visual consolidation against [`docs/design/approved-project-local-ui`](./design/approved-project-local-ui/).

Do not schedule a giant full-app redesign after backend work. UI quality is part of beta readiness, but only beta-critical surfaces need launch polish before the initial Bozeman beta.

## Integrity constraints retained

Response-link activation remains paused after 11.50. Do not activate assignment-detail reveal/copy merely because token infrastructure exists. No service-role shortcut, seed data, production data access, real email sending, or mock/persisted fallback mixing was added by this re-baseline.

## 12.14 provisioning mechanism

12.14 establishes a narrow executable operator boundary rather than a product workspace-creation UI.

- Auth identity remains an operator step: create or invite the approved project-contact user through Supabase Auth administration first. The app's sign-in flow remains invite-only (`shouldCreateUser: false` for normal project-contact sign-in).
- `lib/workspaces/provisioning.server.ts` validates the permanent workspace/contact/grant input and builds a deterministic transaction against the existing `workspaces`, `project_contacts`, and `workspace_contact_grants` architecture.
- `scripts/provision-workspace-access.mjs` can emit the reviewed SQL from an operator-provided JSON file or execute it against local Supabase for validation. It does not read `SUPABASE_SERVICE_ROLE_KEY`.
- `npm run test:bozeman-workspace-provisioning` creates disposable local Auth users, provisions workspace/contact/grant fixtures through the reviewed boundary, verifies RLS-visible access and fail-closed behavior, and cleans up with zero residue.

The provisioning transaction is idempotent only when existing workspace/contact/grant rows match the requested input. It fails closed on conflicting duplicate workspace keys, missing approved Auth users, conflicting contact status, conflicting grants, malformed input, unknown capabilities, or missing `workspace.read`.

Current Bozeman beta capability sets use only existing capability names:

- Main scheduler: `workspace.read`, `questionnaires.review`, `volunteers.view`, `volunteers.edit`, `tasks.view`, `tasks.edit`, `calendar.view`, `calendar.edit`, `assignments.view`, `assignments.edit`.
- Volunteer data entry helper: `workspace.read`, `questionnaires.review`, `volunteers.view`, `volunteers.edit`.
- Scheduling read-only helper: `workspace.read`, `volunteers.view`, `tasks.view`, `calendar.view`, `assignments.view`.

Safe operator procedure for later real Bozeman provisioning:

1. Create or invite the approved project-contact Auth user in Supabase Auth administration.
2. Prepare a local, uncommitted JSON input file with the Bozeman workspace key/display name/lifecycle/timezone/date range/public-intake setting, the approved Auth user id, and the explicit capability set.
3. Run `node --conditions=react-server --no-warnings --experimental-strip-types scripts/provision-workspace-access.mjs --input <file.json> --emit-sql` and review the generated transaction.
4. Apply the reviewed transaction through the approved operator database channel for the target environment.
5. Validate with the normal app Auth sign-in path and the relevant access/regression checks.

No real Bozeman production rows, migrations, generated Supabase type changes, hosted validation, service-role path, seed data, email sending, Calendar writes, volunteer Add/Edit UI, assignment picker, public volunteer lookup, remembered-device behavior, Belgrade migration, or response-link activation was added in 12.14.

## 12.15 manual volunteer Add/Edit path

12.15 establishes the first permanent volunteer-management product path for the Bozeman beta.

- `/admin/volunteers` is now a dynamic/no-store persisted route. It derives the authenticated project contact, deterministic active workspace, and effective grants server-side, requires `volunteers.view` for reads, and requires `volunteers.edit` for manual create/update actions.
- The route no longer uses mock volunteer rows as its user-facing truth source. It renders persisted profiles, a true empty state, a calm unavailable state, and a safe error state without mock fallback or mock/persisted mixing.
- Manual volunteers are legitimate `volunteer_profiles` rows with `profile_source = 'manual'`, null `source_submission_id`, and protected server-derived manual provenance. The browser cannot forge workspace, contact, capability, lifecycle authority outside the allowed form choices, source, or questionnaire provenance.
- Questionnaire-derived profiles remain compatible: `profile_source = 'questionnaire'` preserves `source_submission_id`, and ordinary edits do not erase that provenance.
- Manual create/update goes through authenticated RPCs (`create_manual_volunteer_profile`, `update_volunteer_profile_manual_fields`) that re-check the caller's active project-contact grant and `volunteers.edit` capability. Direct authenticated table insert/update/delete privileges remain denied.
- `npm run test:volunteer-profile-management` performs disposable local validation for create, edit, read-back persistence, view-only behavior, missing-view failure, cross-contact/cross-workspace isolation, malformed/protected input, questionnaire compatibility, no service-role dependency, no secret output, and zero-residue cleanup.

12.15 adds a migration and generated type updates for manual provenance and the two narrow RPCs. 12.15.1 has now completed the required hosted staging validation before any hosted beta dependency relies on that behavior. No real Bozeman production volunteer records, controlled import UI, assignment picker, Calendar write, public volunteer lookup, email sending, remembered-device behavior, service-role usage, seed data, Belgrade migration, or response-link activation was added.

## 12.15.1 hosted staging volunteer profile gate

12.15.1 adds `npm run test:volunteer-profile-management:hosted` as the narrow hosted non-production validation gate for the 12.15 migration/RPC/provenance design.

The command is explicitly locked to the existing approved staging project, `project-local-staging` (`kfuujcfxoayukywvtaeh`), and refuses to run without:

```powershell
$env:RUN_HOSTED_VOLUNTEER_PROFILE_MANAGEMENT_VALIDATION='project-local-staging:kfuujcfxoayukywvtaeh'
```

12.15.1 resumed after the approved staging project was reactivated. Project discovery verified the exact staging ref/name and `ACTIVE_HEALTHY` status, staging was confirmed at `20260705000000` before applying the reviewed pending migration, then advanced cleanly to `20260714121500`. The hosted gate passed hosted generated type comparison, manual profile create/edit RPCs, questionnaire-derived provenance compatibility, `volunteers.view` and `volunteers.edit` enforcement, cross-contact and cross-workspace isolation, revoked/expired/inactive grant behavior, role/title non-authorization, direct table-write denial, malformed/protected input rejection, safe-error checks, and disposable hosted cleanup with exact-run plus namespace zero residue.

The resume required no schema redesign, no service-role application path, no product route change, and no real Bozeman or Belgrade data. The committed generated public-schema types were refreshed from the hosted schema, the volunteer RPC caller was aligned with the generated optional-argument typing, and the hosted gate's static service-role guard was narrowed so false capability/readiness flag names do not fail the test while real service-role usage remains forbidden.

## 12.16 Calendar create/edit scheduled item implementation

12.16 establishes the first permanent scheduled-item write path from the actual `/admin/calendar` product UI. The route remains server-owned, dynamic/no-store, and persisted-truth-only. A contact with the effective `calendar.edit` capability can create and edit a supported one-off timed Calendar item; a contact with only read coverage can still view the Calendar but cannot use working create/edit controls.

The supported 12.16 source path is deliberately narrow: persisted one-off/custom timed items only. The UI does not persist mock task presets, fake preset ids, all-day/date-based authoring, multi-day windows, milestones, recurrence, drag/drop, resize, copy, assignment picker actions, publication, delivery, or response links. 12.17 owns the polished persisted task-preset selector and broader one-off source path.

12.16 adds migration `20260714121600_calendar_item_management.sql`, adds `calendar_items.follow_up_project_contact_id`, relaxes timed/date-based `needed_count` to `0..99`, replaces `create_calendar_item` so new rows derive the Follow-up Contact from the authenticated scheduler, and adds the allowlisted `update_calendar_item_one_off_timed` RPC. Existing Calendar rows are preserved because the Follow-up Contact column is nullable until a reviewed backfill exists. Update preserves the existing Follow-up Contact and assignment truth.

Local validation now includes `npm run test:calendar-item-management`, which proves authorized create/edit persistence, Follow-up Contact, zero-needed timed items, malformed input failure, wrong-contact/wrong-workspace isolation, missing `calendar.edit` failure, direct table-write denial, safe read-model visibility, and zero residue. `npm run test:calendar` now also exercises a browser create -> reload -> edit -> reload round trip against disposable local persisted fixtures.

12.16.1 completed the required hosted non-production validation gate for the Calendar item-management boundary. The gate is locked to `project-local-staging` (`kfuujcfxoayukywvtaeh`) and requires `RUN_HOSTED_CALENDAR_ITEM_MANAGEMENT_VALIDATION=project-local-staging:kfuujcfxoayukywvtaeh`. It verified `ACTIVE_HEALTHY` staging, advanced from `20260714121500` to `20260714121600`, compared hosted generated public-schema types to committed types, validated one-off timed create/edit, Follow-up Contact integrity, needed-count `0` and `0/0 assigned` read-model behavior, protected-field preservation, capability/RLS isolation, grant lifecycle failure, role/title non-authorization, direct table-write denial, malformed schedule/source rejection, fake preset rejection, existing preset-backed source compatibility, legacy nullable Follow-up Contact compatibility, safe output, and exact-run plus namespace zero residue. No product/runtime code change, extra migration, service-role application path, real data, or Calendar feature expansion was added by the hosted gate.

## 12.17 Calendar task-preset selector and one-off definition path

12.17 locally adds the first persisted task-preset source path to `/admin/calendar` without cutting over `/admin/tasks`. The Calendar route remains server-owned, dynamic/no-store, persisted-truth-only, and workspace/contact scoped. Calendar reads still require `calendar.view` plus `assignments.view`; Calendar create/edit still requires `calendar.edit`; the task-preset selector requires the same deterministic workspace context plus `tasks.view`.

The selector uses a new server-only Calendar seam over `task_presets` with explicit columns and no mock fallback. It returns active same-workspace presets only, preserving bounded custom-field definitions and safe system identity while exposing no Calendar occurrence state, assignments, volunteer contact data, raw grants, or broad rows. If `tasks.view` is missing, preset selection is unavailable but custom one-off timed creation remains available for contacts with `calendar.edit`.

12.17 keeps the supported authoring scope narrow: timed scheduled items only. A scheduler may create from a real persisted task preset or from a first-class custom one-off definition. Preset-backed edits can change only occurrence timing, needed count, notes, and empty supported custom values; they preserve the task preset source, Follow-up Contact, workspace, lifecycle, and assignment truth. One-off edits keep the 12.16 title/type/timing/needed/notes path.

12.17 adds migration `20260714121700_calendar_source_selection.sql` with `update_calendar_item_preset_timed`, updates generated public-schema types, adds `npm run test:calendar-source-selection`, and extends the Calendar browser regression to exercise the real preset selector plus preset-backed create/reload/edit/reload.

12.17.1 completed the hosted staging validation gate on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). The gate verified the project as `ACTIVE_HEALTHY`, confirmed migration level `20260714121700` before and after the successful run, compared hosted generated public-schema types to committed types, and validated selector authorization/projection/scoping, active-only lifecycle filtering, missing-`tasks.view` selector unavailability with custom one-off continuity, preset-backed create, source/snapshot semantics, preset-backed edit, source immutability/no conversion, capability and workspace/contact isolation, revoked/expired/inactive grant denial, role/title non-authorization, direct table-write denial for Calendar items and task presets, malformed/source rejection, read-model compatibility, existing one-off compatibility, existing preset-backed row compatibility, safe output, exact-run cleanup, namespace zero residue, and hosted disposable residue count `0`. The only implementation correction during the gate was to the hosted harness's disposable fixture values so they obey existing grant-role and task-preset system-key constraints; no product/runtime/schema behavior changed.

Recommended next slice after the successful hosted gate: `12.18 Volunteer Assignment Picker and Create/Cancel Commands`.

## 12.18 Volunteer assignment picker and create/cancel commands

12.18 locally adds the first permanent volunteer assignment management path to `/admin/calendar`. The Calendar route remains server-owned, dynamic/no-store, bounded by Day/Week/Month/List query ranges, persisted-truth-only, and workspace/contact scoped. Existing Calendar item reads, task-preset source selection, and one-off/preset timed create/edit remain intact.

The route now passes a safe `assignmentPicker` state to the Calendar client. The picker uses `lib/calendar/assignmentPicker.server.ts`, requires the resolved Calendar route context plus `volunteers.view` to show ready volunteer choices, and projects only safe volunteer display fields needed by the inspector: volunteer profile id, display name, and congregation. It does not expose volunteer email, phone, profile notes, questionnaire data, raw grants/capabilities, response tokens, URLs, or provider/database objects.

Assignment create/cancel uses server actions on `/admin/calendar` plus the existing assignment server boundary. Create uses the new authenticated RPC `create_calendar_assignments_batch`, which atomically assigns `1..25` same-workspace active ready volunteers to one active timed/date-based Calendar item, requires effective `assignments.edit`, prevents duplicates, creates `needs_response` current response rows, and rejects malformed, duplicate, wrong-workspace, inactive, on-hold, revoked/expired/inactive-grant, role/title-only, and under-capability contexts. Cancel reuses `cancel_calendar_assignment`, requires `assignments.edit`, and marks active assignments `canceled`.

The Calendar inspector now shows active persisted assignment names and response states (`needs_response`, confirmed, denied), a searchable ready-volunteer picker for contacts with assignment edit access, calm unavailable/error/read-only states, and a non-blocking over-assignment warning. Coverage remains derived from `calendar_assignments` plus current `assignment_responses`; no Calendar counters, mock volunteer arrays, response links, publication state, volunteer-visible schedules, email, delivery, or assignment-detail links were added.

12.18 adds migration `20260714121800_calendar_assignment_management.sql`, updates generated public-schema types for `create_calendar_assignments_batch`, adds `npm run test:calendar-assignment-management`, and updates the Calendar browser regression expectation through the existing product route. Because this slice changes migration/RPC/generated-type behavior, hosted non-production validation was required before 12.19 and completed in 12.18.1.

## 12.18.1 Hosted staging assignment management validation

12.18.1 validates the assignment-management boundary against non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`) at migration `20260714121800`. The gate proves hosted generated-type parity, picker authorization/projection, workspace and lifecycle filtering, atomic batch create, duplicate/retry rejection, over-assignment behavior, response initialization, cancellation, coverage truth, capability/contact/workspace/grant isolation, role/title non-authorization, direct table-write denial, blank-note normalization, compatibility with existing Calendar/assignment behavior, no response-token/email/publication side effects, exact-run cleanup, namespace zero residue, and hosted disposable residue count `0`.

## 12.19 Draft/private versus published/live Calendar visibility

12.19 locally adds the first publication visibility boundary to persisted `/admin/calendar`. Newly created scheduled items now default to `publication_state = 'draft'`, record the authenticated creator as `created_by_project_contact_id`, and keep `published_at`/`published_by_project_contact_id` empty until an explicit publish action. Legacy pre-12.19 rows default to draft without guessed owner metadata, which intentionally fails closed until a later reviewed backfill/publication decision.

Drafts are private to the creating project contact. The Calendar read model projects only published items plus the caller's own drafts, and the client receives only safe publication state, publish eligibility, and published timestamp fields--not raw contact ids, grant ids, capability arrays, or database errors. Published items remain visible to authorized same-workspace contacts through the existing `calendar.view` plus `assignments.view` coverage-bearing read rule.

Publishing uses the new authenticated `publish_calendar_item` boundary. It requires the same deterministic workspace/contact context and effective `calendar.edit`, is one-way/idempotent for the owning draft creator, and records server-derived published metadata. It does not send email, expose a volunteer schedule, activate response links, create response tokens, or treat delivery success as visibility truth.

Assignment preparation remains admin-only. The creator may prepare/cancel draft assignments with `assignments.edit`; other contacts cannot see or mutate the draft. Response-token issuance, replacement, public response reads/submits, and reveal helpers fail closed for drafts and continue only after publication through their existing reviewed boundaries. Response-link reveal/copy UI remains paused.

Local validation uses `npm run test:calendar-publication-visibility`, which proves draft owner-only reads, publish, published cross-contact visibility, assignment/token gating, direct-write denial, grant lifecycle failure, role/title non-authorization, and zero-residue cleanup.

12.19.1 completed the hosted staging validation gate on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). The gate required `RUN_HOSTED_CALENDAR_PUBLICATION_VISIBILITY_VALIDATION=project-local-staging:kfuujcfxoayukywvtaeh`, verified `ACTIVE_HEALTHY` project status, advanced staging from `20260714121800` to `20260714121900` during the first migration application after correcting an unapplied migration comment typo, and the final successful rerun observed `20260714121900` before and after validation. Hosted generated public-schema types now match committed types after a UTF-8-safe refresh.

The hosted gate validated publication defaults, unknown-owner legacy draft fail-closed behavior, server-derived creator and Follow-up Contact metadata, creator-only draft reads, published same-workspace cross-contact visibility, safe publication projection, publish authorization and idempotency, preservation of assignments/source/Follow-up Contact/provenance, draft edit and assignment privacy, assignment-detail privacy, draft response-token/reveal/public-response denial, existing published token/response compatibility, direct table-write denial, cross-contact/cross-workspace/grant lifecycle/role-title denial, malformed input rejection, no email/delivery/volunteer-schedule/public-lookup/remembered-device coupling, safe output, exact-run cleanup, namespace zero residue, and hosted disposable residue count `0`. No product/runtime code changed during the gate.

Recommended next slice after the successful hosted gate was `12.20 Secure Account-Light Volunteer Schedule Access`.

## 12.20 Secure account-light volunteer schedule access

12.20 locally implements the first permanent volunteer-facing schedule path without cutting over `/v/demo` and without reusing assignment-response tokens. The product path is intentionally narrow: an authorized project contact with effective `assignments.edit` can issue or revoke a dedicated volunteer schedule access credential for a same-workspace active ready volunteer profile; the volunteer opens `/v/access/[token]`; the route validates the bearer, sets a session-only HttpOnly `pl-volunteer-schedule` cookie scoped to `/v`, and lands on the clean `/v/schedule` page.

The credential model is separate from `assignment_response_tokens`. Migration `20260714122000_volunteer_schedule_access.sql` adds `volunteer_schedule_access_tokens` with SHA-256 verifier storage only, purpose/version constraints, default 30-day TTL, 1-hour minimum, 90-day maximum, revocation, and last-used tracking. Raw bearers are returned only once by `issue_volunteer_schedule_access`; the database stores only the verifier hash. Revocation through `revoke_volunteer_schedule_access` is idempotent and does not delete assignments, assignment responses, volunteer profiles, or token history.

`/v/schedule` validates the schedule cookie through `read_volunteer_schedule` using a public server-side RPC client and returns only the token volunteer's own published same-workspace assignments. Draft/private Calendar items, archived/canceled items, assignments for other volunteers, unrelated workspaces, inactive/on-hold volunteer profiles, expired/revoked/unknown bearers, and malformed tokens fail closed or render calm empty/unavailable states. In 12.21 the schedule page adds persisted Confirm, Can’t make it with optional notes, eligible response changes, and Confirm All through credential-scoped public RPCs. Those schedule-cookie mutations record `response_source = 'volunteer_schedule'`; the separate `/respond/[token]` assignment-response-token route remains `public_token`. 12.21.1 completed the required hosted staging validation on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`) at migration `20260714122100`.

The browser handoff keeps the bearer out of the clean schedule URL, HTML, localStorage, sessionStorage, and readable cookies. The access route uses a harmless linked handoff state to ensure Chromium/loopback cookie availability before replacing to `/v/schedule`; no bearer is placed in the query string. Cookie attributes are HttpOnly, SameSite=Lax, session-only, Secure on HTTPS, and loopback-compatible for local preview.

Follow-up Contact remains a known projection gap for the volunteer schedule page because the current project-contact schema does not yet contain reviewed volunteer-facing contact display/email/phone fields. The page shows calm copy that project-team contact details will be included in a later beta slice rather than exposing Auth metadata, raw contact rows, grants, capabilities, or private data.

Local validation now includes `npm run test:volunteer-schedule-access` and `npm run test:volunteer-schedule-access:browser`. The local disposable database proof validates hash-only issuance, TTL bounds, safe read filtering, published-only visibility, needs/confirmed/declined response projection, revocation, direct table denial, role/title non-authorization, grant/contact/workspace isolation, malformed/protected input failure, no assignment-response-token coupling, no email/public lookup/remembered-device behavior, no secret output, and zero residue. The browser proof validates the real preview routes, clean bearer exchange, HttpOnly session cookie, persisted schedule display, detail sheet, unavailable/empty states, Not-you cookie clearing, 390px mobile layout, no horizontal overflow, no unsafe leakage, and no `/v/demo` mock leakage.

12.20.1 completed the hosted non-production validation required before 12.21. Do not begin email, remembered devices, public lookup, response-link reveal/copy activation, or real Bozeman data entry until those later reviewed gates exist.

## 12.20.1 Hosted staging volunteer schedule access validation

12.20.1 completed the hosted staging gate for the volunteer schedule access boundary on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). The target was verified as `ACTIVE_HEALTHY`, and staging was verified at migration `20260714122000` before and after the successful run.

The gate added `npm run test:volunteer-schedule-access:hosted`, locked by the exact opt-in `RUN_HOSTED_VOLUNTEER_SCHEDULE_ACCESS_VALIDATION=project-local-staging:kfuujcfxoayukywvtaeh`. It validated hosted generated public-schema type parity after a UTF-8-safe refresh from hosted output; dedicated token separation from assignment-response tokens; hash-only verifier storage; TTL defaults and bounds; server-derived issuer/workspace/volunteer scope; issuance/revocation authorization through effective `assignments.edit`; public ready/ready-empty/unavailable reads; published-assignment filtering; current-response truth; safe projection; direct table denial; cross-contact, cross-workspace, and revoked/expired/inactive grant isolation; role/title non-authorization; inactive/on-hold volunteer failure; malformed/protected input rejection; multiple credential independence; last-used behavior; hosted `/v/access/[token]` to `/v/schedule` exchange; clean final URL; HttpOnly SameSite=Lax session cookie; request host preservation; Not-you clearing; 390px layout; focus behavior; safe output; exact-run cleanup; namespace zero residue; and hosted disposable residue count `0`.

Product/runtime code did not change during the 12.20.1 gate. Confirm/Deny and Confirm All were added later in the local 12.21 slice and hosted-validated in 12.21.1. Email, remembered devices, public lookup, `/v/demo` cutover, response-link reveal/copy activation, and real Bozeman/Belgrade data remain out of scope.

## 12.21.1 Hosted staging volunteer response round-trip validation

12.21.1 completed the hosted staging gate for the volunteer schedule response boundary on non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`). The target was verified as `ACTIVE_HEALTHY`; staging was already at migration `20260714122100` before the gate and remained at `20260714122100` after validation.

The gate added `npm run test:volunteer-schedule-responses:hosted`, locked by the exact opt-in `RUN_HOSTED_VOLUNTEER_SCHEDULE_RESPONSE_VALIDATION=project-local-staging:kfuujcfxoayukywvtaeh`. It validated generated public-schema type parity after normalizing non-semantic Supabase generator metadata, schedule Confirm, Can’t make it with bounded notes, eligible reconfirmation, Confirm All, 48-hour decline cutoff, assignment-start lock, `volunteer_schedule` provenance, `/respond/[token]` `public_token` parity, token separation, public ready/empty/unavailable reads, schedule filtering, admin Calendar response truth, safe projection, direct table denial, cross-token/cross-volunteer/cross-workspace isolation, hosted route actions, clean URL, HttpOnly cookie attributes, Not-you clearing, 390px layout, no email/public lookup/remembered-device/response-link side effects, exact-run cleanup, namespace zero residue, and hosted disposable residue count `0`.

Product/runtime code did not change during the hosted gate; only the hosted validation harness, package script, narrow historical browser guardrail update, and docs changed. Recommended next slice after this successful hosted gate: `12.22 Initial Assignment Notification Email Boundary`.

## 12.22 Initial assignment notification email boundary

12.22 locally adds the first narrow initial assignment notification boundary to `/admin/calendar`. The action is explicit and scheduler-controlled; it does not send on render, page load, GET, publication, assignment creation, hover, focus, prefetch, or client effects.

The server action derives the authenticated project contact, deterministic workspace context, and effective assignment mutation authority server-side. Send eligibility is re-read from persisted truth at send time: the Calendar item must be active, published, same-workspace, future/not-started, and have active assignments for active ready volunteers with valid email addresses. The current strict admin edit context is `assignments.edit`; the browser supplies only the Calendar item id and redirect metadata, never workspace id, contact id, capabilities, recipient data, provider fields, token values, or Follow-up Contact authority.

Migration `20260714122200_initial_assignment_notifications.sql` adds volunteer-facing Follow-up Contact fields to `project_contacts`, adds the `assignment_notification_deliveries` ledger, and adds authenticated RPCs for safe summaries, send claiming, and delivery finalization. The ledger stores bounded delivery metadata only: no bearer, full URL, verifier, response URL, raw provider dump, SQL, stack trace, grant/capability array, or credential material. Successful initial delivery for the same assignment/kind/template version is terminal; failed attempts may be retried; in-progress claims expire so they can recover. Direct authenticated table writes remain denied.

The email provider is server-only and replaceable. It is disabled by default and the implemented deterministic local transport records redacted JSONL delivery summaries for validation. Schedule access uses the existing `/v/access/[token]` boundary and the existing hash-only volunteer schedule access token table; the bearer is used only in memory to build the outbound link and is never persisted in the notification ledger or rendered in the admin UI. If provider send fails after token issuance, the helper attempts to revoke the issued schedule credential before finalizing failure.

Follow-up Contact volunteer-facing data is now explicit. 12.22 does not use Auth metadata or raw project-contact internals as volunteer-facing contact details. Existing Calendar items without Follow-up Contact volunteer-facing display/email data are skipped with safe status rather than sending incomplete messages.

The Calendar inspector now includes an Initial email card with ready/already-sent/missing-email/missing-Follow-up-Contact counts and one explicit send button when the transport is configured and eligible recipients exist. Publishing remains separate from email; assignment creation/cancellation remains separate from email; response-link reveal/copy remains paused; no Communications module, reminders, public lookup, remembered devices, assignment-detail links, provider integration, real Bozeman data, Belgrade migration, or service-role application path was added.

Local validation uses `npm run test:assignment-notification-email`, which proves claim/finalize delivery behavior, duplicate prevention, missing recipient and missing Follow-up Contact handling, explicit send action wiring, schedule-access issuance, safe recording output, direct table denial, wrong-contact/wrong-workspace/role-only failures, disabled transport behavior, and zero disposable residue. 12.22.1 completed the required hosted staging validation gate before 12.23.

## 12.23 Bozeman beta launch gate

12.23 adds the launch-gate contract and operational package for the initial Bozeman beta candidate.

- Focused UI polish was applied to the persisted Calendar inspector/assignment picker/Initial email card, persisted Volunteers Add/Edit surface, and volunteer schedule response states.
- `lib/readiness/bozemanBetaLaunchGate.server.ts` is server-only and route-unused. It cannot launch production, create data, send email, mutate deployment configuration, expose secrets, use a service-role application path, or activate response-link reveal/copy.
- `npm run test:bozeman-beta-launch-gate` proves the launch gate remains route-unused and honest.
- `npm run test:bozeman-beta-ui` composes the focused loopback browser suites for Calendar, Volunteers, and volunteer schedule mobile/desktop behavior.
- `npm run test:bozeman-beta-launch:hosted` and `npm run test:bozeman-beta-e2e:hosted` are exact-opt-in only for `project-local-staging` (`kfuujcfxoayukywvtaeh`) and now require the current `ACTIVE_HEALTHY` staging terminal migration `20260811123300`. The corrected launch verification passed with type parity and no fixtures; the full disposable E2E loop passed again with recording-only email and exact/namespace/Auth zero residue. Historical 12.23/12.23.1 evidence remains recorded at `20260714122230`.
- `docs/BOZEMAN_BETA_LAUNCH_RUNBOOK.md` records the operator procedure, environment inventory, Auth/workspace/grant procedure, volunteer/calendar/email operations, observability, backup/recovery, pilot, stop conditions, and Belgrade fallback.
- `docs/BOZEMAN_BETA_GO_NO_GO.md` records the conclusion: `NO-GO`.

The NO-GO is intentional and evidence-based: the persisted technical loop, product-owner-approved beta-critical UI, Resend configuration, direct provider-level inbox delivery, 12.32 privacy-safe application observability foundation, 12.32.1 Vercel runtime-log/controlled-event evidence, 12.33 initial-beta operator notification-health architecture, and complete backup/recovery are proven. Project Local application-driven production email, real operator provisioning, production migrations/Notification Health execution, and controlled pilot requirements are not yet evidenced. No real Bozeman data, application assignment email, Belgrade migration, service-role application path, public lookup, remembered-device behavior, full Communications, `/admin/tasks` cutover, `/v/demo` cutover, response-link reveal/copy activation, or assignment-detail entry link was added by the observability or backup-reconciliation slices.

## 12.31 Production transactional email provider integration

12.31 selects Resend and extends the existing server-only 12.22 provider abstraction rather than creating a second email architecture. Disabled behavior remains the default, recording remains deterministic QA transport, and `resend` is an explicit production-capable option that requires the trusted notification origin, approved sender, and server-only `RESEND_API_KEY`. Missing or malformed values and unknown transports fail closed without fallback.

The adapter calls Resend's `POST /emails` endpoint directly from server code, maps only the existing persisted assignment message contract into concise Project Local text/HTML, and sends the schedule URL only in the provider request. A deterministic provider idempotency key is derived by hashing the database-issued non-secret ledger key; it contains no recipient, bearer, full URL, or API key. The database ledger remains authoritative for successful-send terminal state, duplicates, retry, and stale recovery. Only a bounded provider message id is returned for finalization; non-2xx, network, malformed JSON/id, and unexpected provider behavior become `provider_send_failed` with no raw provider response exposure.

`npm run test:assignment-notification-email:resend` uses dependency-injected fake responses and never contacts Resend. Existing local ledger regression remains responsible for duplicate prevention, failure retry, stale recovery, credential revocation, recording behavior, and zero residue. The safe application boundary now exposes typed claim/configuration failure stages and aggregate provider/finalization/schedule-access failure counts for later observability without changing current Calendar presentation.

During 12.31 no real email was sent, and no Resend account, domain, sender, key, DNS record, Vercel environment variable, webhook, analytics, migration, RPC, RLS, generated type, Auth rule, schedule credential/cookie, hosted database, production data, or 12.30.1-approved UI was changed. At the end of that iteration, real activation remained blocked on domain/sender verification, a least-privilege server secret, canonical origin, disabled tracking, safe monitoring, an approved test recipient, and recorded deliverability proof. The later 12.31.1 evidence update below records which provider-level prerequisites were subsequently proven while retaining the application gate and `NO-GO` decision.

## 12.31.1 Resend operator configuration and provider deliverability evidence

August 10, 2026 operator evidence confirms the Resend account is configured, `projectlocal.app` is verified and ready as a sending domain, `Project Local <notifications@projectlocal.app>` is the verified sender, and a restricted Sending-access key scoped to the domain is stored only in encrypted Vercel Production settings. Vercel Production also contains the canonical notification base URL and verified sender. Resend open and click tracking are off. No credential value is recorded in the repository.

A direct Resend-dashboard test message from the verified sender arrived in an approved Gmail inbox. That proves provider/domain/sender/basic inbox deliverability only. It did not invoke Project Local's Initial email action, claim or finalize an `assignment_notification_deliveries` row, issue or exercise an app-generated schedule-access link, prove application duplicate behavior, or exercise retry/failure operations.

`ASSIGNMENT_NOTIFICATION_EMAIL_TRANSPORT=resend` was temporarily enabled only after provider readiness; no Project Local assignment email was sent while it was enabled. The transport was removed, Vercel redeployed, and the resulting `https://projectlocal.app` production deployment is Ready/Latest. `ASSIGNMENT_NOTIFICATION_EMAIL_TRANSPORT` and `ASSIGNMENT_NOTIFICATION_RECORDING_PATH` are currently absent, so application email is disabled. No production workspace, contact/grant, volunteer, Calendar item, assignment, delivery row, or other real Bozeman product data was created.

The Initial Assignment Email launch gate therefore remains blocking. A separately reviewed controlled app-driven test must prove the actual claim/provider/finalize round trip, duplicate protection, schedule-access behavior, retry/failure operations, and credential-free monitoring. Launch remains `NO-GO`.

## 12.32 Production observability foundation

12.32 adds the smallest application-owned observability layer for the beta-critical loop. `lib/observability/server.ts` is server-only and accepts only catalogued event names, event-specific safe failure codes, and an optional UUID correlation id. Severity, category, stage, outcome, schema version, and timestamp are derived internally. Arbitrary fields, raw `Error` values, tokens, URLs, and unknown codes are rejected; a failed logger cannot change the original operation.

Representative Auth callback, Calendar, Volunteer, assignment, schedule-access, individual response, and Confirm All failure boundaries now emit safe operational outcomes without changing their user-facing copy. The assignment-email boundary distinguishes configuration, claim, schedule-access, provider, finalization, and sent outcomes while the database ledger remains authoritative. `lib/observability/staleAssignmentDeliveries.server.ts` detects injected expired `sending` projections without querying or mutating a database, retrying, sending, scheduling, or using service-role access.

`npm run test:production-observability` proves the bounded schema, privacy rejection contract, deterministic capture seam, logging-failure isolation, email-stage distinction, successful send event, stale detection, representative route wiring, server-only boundary, no Client Component import, and no observability table or service-role path. No external telemetry, hosted access, real email, schema/RPC/RLS/Auth/security/UI/backup change, or product data was added.

The application observability foundation is now proven. The later 12.32.1 evidence below records the narrow Vercel production review proof while preserving the unresolved overall gate. See [`PRODUCTION_OBSERVABILITY.md`](./PRODUCTION_OBSERVABILITY.md). Launch remains `NO-GO`.

## 12.32.1 Production operator observability evidence

August 11, 2026 operator evidence proves that live Project Local request/runtime entries are available in Vercel -> `project-local` -> Logs with search, warn/error/fatal, and route/request/environment/status filters. Deployment/build failures are reviewed in Vercel -> `project-local` -> Deployments and the selected deployment's build status/logs. The Project Local product/operator owner is the primary alert and incident-response owner and may use Codex or engineering help without transferring that responsibility.

A controlled GET to `https://projectlocal.app/v/access/not-a-real-token` returned the expected redirect/unavailable behavior, created no data, sent no email, and produced a searchable structured `schedule_access.exchange_failure` warning with category `schedule_access`, stage `exchange`, and outcome `failure`. The observed entry exposed no volunteer identity, bearer/token/full URL, Auth/session credential, provider payload, raw provider/Supabase error, SQL, grant/capability array, API key, or environment secret.

Immediate investigation is required for fatal events, repeated errors, repeated beta-critical Auth/Calendar/Volunteer/assignment/schedule-access/response failures that impede normal operation, and any assignment-email provider/finalization failure after application email is later enabled. Pause affected operations for cross-workspace or wrong-volunteer exposure, credential/full-URL leakage, duplicate external assignment email, corrupted Confirm/Deny truth, unrecoverable production mutation, or provider/security misconfiguration.

At the end of 12.32.1, overall observability remained blocking. The authorized read path, review cadence, threshold/escalation proof, and practical manual notification mechanism had not yet been implemented. That historical truth is superseded by the 12.33 architecture and staging evidence below; 12.32.1 itself added no application code, data, email, external configuration, paid service, webhook, browser tracking, analytics, cron, or background job. Launch remained `NO-GO`.

## 12.33 Authenticated assignment notification health

12.33 adds one no-argument authenticated read RPC. It derives `auth.uid()` and exactly one active workspace through an active contact and current, non-revoked grant with `workspace.read`, `calendar.view`, `assignments.view`, and `assignments.edit`. Missing or ambiguous eligibility, role-only authority, missing capability, inactive contact/workspace/grant, and revoked or expired grants fail closed. The `SECURITY DEFINER` function uses an empty search path, exposes only delivery id/state/expiry for up to 100 `sending` rows ordered earliest-expiry first, grants execute only to authenticated, preserves direct ledger revocation and FORCE RLS, and performs no mutation.

The server-only reader calls only that RPC, validates the exact safe projection, and reuses the 12.32 stale detector. One operator check emits at most one existing stale-delivery signal. The unlinked dynamic/no-store `/admin/diagnostics/notification-health` route requires the same operational workspace/capability authority and displays only healthy, stale-count, or unavailable states.

Local disposable proof and hosted `project-local-staging` proof through migration `20260811123300` cover generated-type parity, authorization, ambiguous workspace denial, wrong-workspace isolation, revoked/expired/inactive denial, minimal sending-only projection, 100-row oldest-first behavior, stale/fresh/empty states, direct table denial, no mutation, and zero residue. No email was sent and production was not accessed.

While application email is disabled, routine checking is unnecessary. Once enabled for a controlled test or pilot, check after each email test/batch, before manual retry, and at the end of each active email day. Any stale row requires immediate ledger reconciliation; repeated stale rows or more than one unresolved row pauses sending. This manual check, Vercel runtime-log workflow, cadence, and named ownership are sufficient for the initial tiny controlled beta. Automated alerting is not required or claimed. Operator observability architecture is proven; production RPC execution, real Bozeman grants, and production stale-row observation remain later production/pilot evidence. Application email remains disabled and launch remains `NO-GO` for the remaining email, backup/restore, provisioning, and pilot blockers.

## 12.34 Independent production backup execution and local restore proof

The DPAPI secret ACL is proven current-operator-only. A read-only Session Pooler preflight proved exact production ref `wdlaauzknfggoqldolmx`, database `postgres`, and terminal migration `20260714122230`. One real backup execution produced the unchanged six-file logical package and persisted only its age-encrypted form in private OneDrive. Credential-free status records `2026-08-12T17:26:46.3144615Z`, `62409` bytes, and a matching SHA-256; retention recognition and plaintext cleanup passed.

The reviewed loopback disposable restore stopped at `roles.sql` before schema, migration history, or data. A synthetic local Supabase role-only dump reproduced the managed-role privilege class; the local restore connection role is not a superuser, while all seven required platform roles already exist. Per the backup contract, `roles.sql` was not skipped or removed. A separately reviewed role-application rule and complete restore/post-restore verification remain required. Both disposable stacks and decrypted workspaces were removed. Production stayed at `20260714122230`; staging, email, Vercel, Resend, DNS, Auth, product data, and migration `20260811123300` were untouched. Launch remains `NO-GO`.

## 12.34.1 Supabase managed-role restore compatibility and full local restore proof

The existing 12.34 encrypted artifact was reused without production/staging access or another backup. Its unchanged `roles.sql` contains eight fully supported statements: three session settings, one reset, three `statement_timeout` settings for managed roles, and one managed parameter privilege. No user role, password/verifier, ownership statement, or unknown statement is present. Temporary derived SQL verifies `anon`, `authenticated`, `authenticator`, and `supabase_realtime_admin` plus the represented settings/privilege instead of recreating platform infrastructure. Synthetic user-role create/property/credential/configuration/membership material remains deterministic and temporary; unsupported or privilege-sensitive SQL fails closed.

Exactly one fresh loopback-only restore passed artifact/member validation, managed roles, schema, all 23 migrations through `20260714122230`, data, baseline public functions, absence of the pending Notification Health RPC, RLS on all 13 Project Local tables, and expected FORCE RLS. The next security check found 26 direct `TRUNCATE` grants: both `anon` and `authenticated` across all 13 tables. Because `TRUNCATE` bypasses RLS, the drill stopped without normalization or retry. Production-baseline type parity, product-row state, and remaining application compatibility checks were not reached. The logical dump represents PostgreSQL Auth schema/data and Storage metadata, but not Supabase Auth platform configuration or Storage object BLOBs. All decrypted/transformed/temp material and the disposable stack were removed. Recovery, scheduling/failure notification, and recovery ownership remain blocking; application email is disabled and launch remains `NO-GO`.

## 12.34.2 Restored grant attribution

A controlled local attribution probe proved the 26 direct `TRUNCATE` grants were introduced by `RESTORE_INTERACTION`, not present in the historical source ACL. This removed the false source-security attribution without waiving the privilege mismatch or changing the six-file package.

## 12.34.3 Deterministic source ACL recovery and hosted staging privilege hardening

The recovery path now parses the real encrypted artifact's historical table ACL statements, validates the complete 13-table shape and recognized privilege classes, and reconstructs the exact source ACL after restore. A full local recovery-forward completed through both pending migrations, ending at 25 versions through `20260812123430`. Generated public-schema types, all-table RLS, the exact four FORCE RLS tables, no `anon`/`PUBLIC` direct privileges, `authenticated` `SELECT` on exactly nine approved tables, protected postgres future-table defaults, product-row state, Notification Health, Calendar/assignment, Volunteer, volunteer-schedule behavior, and zero fixture residue passed. Full independent technical recovery is proven.

Migration `20260812123430` codifies the exact direct/default privilege posture. Approved non-production `project-local-staging` (`kfuujcfxoayukywvtaeh`) advanced from `20260811123300` to `20260812123430`; hosted metadata, generated-type parity, RLS/FORCE RLS, Notification Health, a transactionally rolled-back future-table probe, and zero fixture/Auth/object residue passed. Production was not accessed and remains recorded at `20260714122230`; neither later migration has been applied there. Application email remains disabled. Recurring backup scheduling, safe failure-notification proof, recovery/rollback ownership, app-driven production Initial Email proof, real Bozeman provisioning, and controlled pilot remain blocking. The logical database package still does not prove Supabase Auth platform configuration or Storage object BLOB recovery. Launch remains `NO-GO`.
