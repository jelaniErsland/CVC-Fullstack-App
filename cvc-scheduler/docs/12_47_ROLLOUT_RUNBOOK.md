# 12.47 controlled rollout runbook (historical)

**Status: historical reviewed procedure, executed for the authorized 12.47 rollout on 2026-09-24.** The four migrations, backup-lock transitions, exact release deployment, application-grant restoration, live smoke and independent current-terminal recovery proof passed. Production is at `20260922150000`, the backup task is Enabled/Ready with the same lock, Vercel Production is Ready at `5bb1cf6a0609285c7c4c53d4e3516727b67adbac`, and Standard Protection has been restored. The public guide is live at `https://projectlocal.app/guide`. Photo uploads remain disabled; no volunteer email campaign was sent. See [current state](CURRENT_STATE.md) and [production readiness](12_47_PRODUCTION_READINESS.md) for closeout evidence. The steps below document the reviewed sequence and do not authorize any new production action.

## September 24 read-only starting evidence — verify again at rollout

The live DB was inspected read-only at terminal `20260908130000` with 39 migration records, 18 RLS-enabled public tables, and the exact deployed 58-function policy (8 anonymous / 38 authenticated / 12 internal). No 12.47 migration was live; the source has only the four pending files in step D. Current aggregates were 103 volunteers, 77 Calendar items, 23 assignments/responses, 26 schedule credentials, 18 notification deliveries, nine Auth users/identities, nine contacts/grants, and zero Storage buckets/objects; checked orphan counts were zero. These are historical observations, **not** rollout acceptance counts. One assignment updated after the prior snapshot; obtain a new repeatable-read baseline at the authorized quiet-window boundary and investigate any later changes before checkpoint/migration.

The permanent backup task was Enabled/Ready/not running with lock `20260908130000`, reviewed daily 03:15 action and successful September 24 result. Read-only recovery preflight passed. The latest encrypted `.age` artifact was 129,106 bytes with an independent SHA-256 match and no plaintext/partial/temp residue. No backup was executed for this review. `origin/master` matched baseline HEAD `de667eea158df44d99e7b31d95a089cb7440c5b4`; the public site returned HTTP 200. The owner supplied a Vercel deployment-details screenshot showing Production/Ready from `master` at abbreviated SHA `de667ee`, matching the baseline prefix. The full deployed SHA was **not independently available** through authorized read-only metadata. Before authorizing step B, an authorized owner must confirm the exact full deployed SHA and the seven intended Production variable names/scopes; presence alone does not prove working delivery or recovery.

The owner observed the seven Production variable names and the Supabase Auth/SMTP/signing, Resend-domain/key-record settings listed in [readiness](12_47_PRODUCTION_READINESS.md#platform-configuration-recovery-inventory--owner-confirmed-custody-untested-replacement). Their hidden values and key-to-consumer mapping were not independently verified. The owner has personally confirmed account/MFA, domain/DNS, credential-reissuance and independent encrypted-backup/age-identity custody arrangements; retain the [redacted operator recovery checklist](12_47_AUTH_STORAGE_RECOVERY.md#external-configuration-recovery-owner-held-procedure). Do not infer a successful email or fully exercised disaster recovery from dashboard setting presence or custody attestation.

## Pre-release gates and ownership

The release operator owns migration, exact deployment and observation. The backup/recovery operator owns checkpoint, lock, independent restore proof and task state; a second reviewer verifies hashes and catalog comparisons. The product owner separately authorizes the rollout, optional photo enablement, a controlled email recipient and any real campaign. Record date/time, operator, exact source commit, artifact hash and safe machine-readable evidence for each gate. Never record credentials, private volunteer fields, bearer URLs or full outgoing email content.

Before scheduling: review the exact 37-signature `service_role` replay correction in the last unapplied migration and its [per-function evidence](12_47_ACL_RECONCILIATION.md). Prove a clean catalog is exactly 72 (10 anonymous, 47 authenticated-only, 15 internal) with exact per-signature grants, PUBLIC and default PUBLIC/anon/authenticated EXECUTE zero, SECURITY DEFINER owner and pinned search path. Review the local four-step backup-lock contract `20260908130000 -> 20260922120000 -> 20260922130000 -> 20260922140000 -> 20260922150000`. The three intermediate locks are valid **only while the permanent task remains Disabled**; the helper refuses backup execution, registration and enablement there. Prove wrong old lock, lock ahead, skipped, arbitrary future and malformed terminals fail closed. The existing real artifact and a new local encrypted package passed the [reduced-scope Auth/empty-Storage recovery procedure](12_47_AUTH_STORAGE_RECOVERY.md) in two pinned Supabase-initialized targets. Sessions are intentionally omitted; photo BLOB recovery remains a separate feature gate. Have the responsible operators verify the redacted [platform configuration inventory](12_47_PRODUCTION_READINESS.md#platform-configuration-recovery-inventory--owner-confirmed-custody-untested-replacement): Supabase Auth site/redirect/provider/mail settings and signing/key custody, Vercel environment names and scopes, and Resend identity/key custody. No test send at this gate. Review the [exact release file manifest](12_47_RELEASE_SCOPE.md), including `public/guide` and its `/guide` rewrite/navigation. Exclude generated runtime files, local fixtures and all review captures. Obtain one reviewed application release commit before deployment. Any missing gate means STOP.

## Interpreting the real-backup rehearsal

The 2026-09-23 real artifact decrypted with the existing local recovery identity; its manifest names terminal `20260908130000`. After the earlier application-scoped proof, a fresh platform-initialized target restored nine exact real Auth users and nine provider identities, all contact/grant relationships, current empty Storage metadata and application data. The four migrations preserved all 18 old-column fingerprints and passed 72-function ACL/RLS checks. An encrypted expanded package then restored independently into a second fresh platform target, with all 26 public table fingerprints and essential Auth data hashes matching. The source package has no Auth/Storage table definitions: initialize compatible managed schemas first, never use the old 23-migration/13-table restore script as acceptance evidence, and follow [the precise restore order and exclusions](12_47_AUTH_STORAGE_RECOVERY.md). A separate fresh synthetic-fixture Next.js smoke passed the seven admin routes, enforced sign-in routing and volunteer home; it was not a browser test of the restored production-copy target. No old session is promised; no real login email was sent. Site/redirect URLs, provider and mail settings, signing material, project keys and deployment environment must be recreated and checked from separate operator custody during a real recovery; the logical backup cannot prove that external configuration by itself.

## A. Fresh read-only preflight and write pause

Human approval gate A: authorize a maintenance window and non-destructive read-only preflight. Confirm the exact production project/ref and workspace identity, current DB terminal **`20260908130000`**, four and only four pending local migrations, permanent task Enabled/Ready/not running with lock `20260908130000`, unchanged action/schedule/destination/principal, recovery GREEN, current deployed commit, actual email configuration and absence of unexpected storage uploads. Confirm the approved encrypted backup path and independently verifiable artifact, but do not start another task during inspection. Capture current volunteer, assignment, response, credential, notification, contact/grant, Calendar and Auth counts plus row-content hashes in one repeatable-read read-only snapshot. Verify FK/orphan checks, RLS and exact baseline 58-function catalog (8/38/12). Do **not** compare to an assumed volunteer count; other authorized contacts may have edited data. If the live schema, source, privileges or task diverge, STOP and re-review the migration against reality.

Before step B, block application/admin writes or otherwise establish an operator-verified quiet window. Vercel Authentication set to All Deployments is only an ingress gate for visitors without Vercel deployment access. It does not block eligible Vercel users/shareable or automation bypasses, direct Supabase API calls by holders of valid credentials, or out-of-band/background writers. An unauthenticated redirect therefore does **not** prove a universal write pause. The owner's coordinated no-write window and repeated fingerprints detect unexpected activity but do not themselves prevent writes. The [locally rehearsed one-time schema gate](12_47_MAINTENANCE_CONTROL.md) covers reviewed `anon`/`authenticated` project writes after the normal-permission checkpoint; it requires fresh live ACL/writer inventory and separate owner approval before use. If any external service-role/postgres writer cannot be ruled out, or the live grant topology differs, STOP rather than improvising broad revocations. Obtain a fresh read-only fingerprint at the boundary and immediately before/after the checkpoint and each migration. If rows changed since A, investigate expected writes and recapture the baseline; if writes cannot be stopped or explained, STOP. No email campaign during migration.

## B. Independent pre-migration checkpoint

Human approval gate B: authorize exactly one controlled encrypted checkpoint at `20260908130000` **before** the temporary schema gate changes permissions, using the reviewed temporary triggerless Scheduled Task technique with the permanent task's exact action, current-operator Interactive/Limited principal and no raw `ProcessStartInfo`. Before registration, require no previous temporary checkpoint task or stale marker. After registration but **before starting it**, use `scripts/production-backup/ProjectLocalCheckpointPrestart.psm1` against the reread candidate, its `Get-ScheduledTaskInfo` result, the reread permanent task's single exact action, the current operator SID and an absolute marker path. Require `Pass=true`; the guard filters null trigger values, compares exact action including working directory, resolves the task principal to a SID, requires Interactive/Limited and Ready/Enabled/IgnoreNew settings, and rejects a previously run task or stale marker. Independently require the permanent task, database terminal/lock, recovery status, maintenance controls and data fingerprint still match the preflight. A failed guard means unregister only the unstarted temporary task, inspect the cause and STOP; never start it to investigate. Start a passing temporary task once; no automatic retry. Require success result, one new nonzero `.age` artifact, independent SHA-256 match, decrypted manifest `ExpectedMigration=20260908130000`, zero plaintext/partial/temp residue, temporary task removed and permanent task unchanged. Independently restore the **new** checkpoint into a pinned isolated target, including essential Auth and current Storage metadata, and require the normal pre-maintenance schema/object ACLs and data fingerprint before step C. Reverify no writes occurred between the frozen fingerprint and checkpoint. If the task fails or the artifact/manifest/restore proof is uncertain, STOP with permanent task still Enabled/Ready and no migration. Do not claim checksum alone proves restoration.

## C. Backup task and lock safety

Human approval gate C: with the task not running, use only `scripts/production-backup/Register-ProjectLocalBackupTask.ps1` reviewed `Inspect`/`Disable` actions. Record exact pre-disable action, schedule, destination and principal. Validate the **first** proposed lock transition read-only while the task is still Enabled/Ready, requiring `MutationPerformed=false`. Disable only the permanent task; verify Disabled/not running and lock still `20260908130000`. Then snapshot the exact live schema/object ACLs and apply the separately approved [one-time schema gate](12_47_MAINTENANCE_CONTROL.md), requiring both application roles to lose effective `public` schema USAGE while `service_role` and `postgres` retain it. Because PostgreSQL warns that already-open sessions may retain prior object lookups, identify and recycle only reviewed idle application database backends, verify their old PIDs are gone and the reconnected API denies a harmless mutation call. Unknown or active writers are STOP, not candidates for blind termination. The pre-migration checkpoint must have captured **normal** permissions and passed independent restore first. Keep the schema gate through all four migrations and deployment; recheck it at each terminal. Later lock transitions use the already-reviewed exact contract plus fresh read-only database/task inspection; the helper's production `ValidateExpectedMigrationTransition` intentionally requires an Enabled/Ready task and is not a workaround for a disabled task. Do not edit Scheduled Task XML or manually start it. If task or schema-gate state changes unexpectedly, STOP before migration.

## D. Four forward migrations

Human approval gate D: apply **only**, in this order, to the exact production project:

1. `20260922120000_bulk_calendar_assignments.sql`
2. `20260922130000_communication_delivery_operations.sql`
3. `20260922140000_volunteer_csv_import.sql`
4. `20260922150000_project_hero_volunteer_home.sql`

For **each** row below, apply only that file, require the exact terminal and the immediately preceding lock to classify `migration_lock_transition_pending`, check expected schema/functions, unchanged pre-existing row fingerprints and continued denial of effective `public` schema USAGE to `anon`/`authenticated`, then use the reviewed `UpdateExpectedMigration` helper for **only that adjacent pair** while the permanent task stays Disabled/not running. Inspect again: DB terminal and task lock must match exactly before continuing to the next row. Record every result. No backup run or task enablement at the three intermediate locks.

| Applied file terminal | Lock before | Lock after, only after verification | Continue condition |
| --- | --- | --- | --- |
| `20260922120000` | `20260908130000` | `20260922120000` | Bulk tables/RPC and old data sound; task Disabled/not running. |
| `20260922130000` | `20260922120000` | `20260922130000` | Communication tables/nullable ledger link sound; task Disabled/not running. |
| `20260922140000` | `20260922130000` | `20260922140000` | Import function replacements and old profile/history sound; task Disabled/not running. |
| `20260922150000` | `20260922140000` | `20260922150000` | Photo/away/menu schema, exact ACL and all preservation checks sound; task Disabled/not running. |

Do not edit an applied migration. Each file is transaction-wrapped; a failed file should roll back its own work, but previously completed files remain. At **any** failure or contradictory result, STOP with the permanent task Disabled; do not advance the lock before verifying the corresponding database terminal. Do not run an old-lock or intermediate-lock task, skip a transition, run a blind SQL down migration or deploy the new app. Use a separately reviewed forward correction or a **proven real restore** to a new environment; a SQL rollback is not a recovery plan. Coordinate restoration of Auth/configuration and any object storage separately.

## E. Immediate post-migration gate, lock and recovery

Before app deployment, in the quiet window, compare the frozen pre-migration counts and content hashes for every pre-existing relation. For the nullable notification-recipient link, compare historical row fields excluding that new null column; prove its existing rows remain present and unchanged. Verify all volunteer, private profile, assignment, response, schedule-credential, notification, Calendar, contact/grant and Auth relationships; orphan count zero; workspace and project dates unchanged. Confirm new tables are empty except intentional deterministic seed state, RLS and indexes/constraints are present, uploads are disabled, and no unsolicited email was sent.

Independently enumerate the exact 72 public signatures: 10 anonymous, 47 authenticated-only, 15 internal; unexpected anonymous/PUBLIC and default PUBLIC/anon/authenticated EXECUTE zero; expected `service_role` grants exact; SECURITY DEFINER owner/search path and representative cross-workspace/credential privacy checks pass. A fixture count alone is insufficient. Any mismatch: STOP, leave task disabled, preserve evidence, and do not deploy.

Human approval gate E: after the fourth exact adjacent transition, require DB **and** task lock `20260922150000`, full preflight/recovery GREEN, and every security/data check above. Then use the reviewed `Enable` action without starting the task. Verify Enabled/Ready/not running, matching lock and unchanged action except lock, schedule, destination and principal. The helper denies enablement at the three intermediate terminals. If recovery or re-enable fails, STOP; do not manually patch the task or invoke another backup. The helper must already have passed production-independent transition regressions before this gate.

## F. Application deployment

Human approval gate F: only after E passes, integrate the exact reviewed **local release commit, including `public/guide`**, into `master` and push normally through the separately authorized pipeline. Vercel Production tracks `master`: **do not push the new application code to master before the four migrations, final policy/data checks and backup-task reenablement succeed**. Confirm Vercel Production/Ready at the exact commit and `https://projectlocal.app`; no force push. Migrations precede the new app because the new code calls new tables/RPCs. Keep Vercel protection and the schema gate active through deployment, so the old application cannot write against the final database. Do not expose a mixed new-app/old-DB version. If deployment is not Ready or targets the wrong commit, STOP with both gates in place, keep sending/campaigns paused and investigate; never improvise a database downgrade.

## G. Non-destructive live smoke

Human approval gate G: only after the exact deployment is Ready, restore the snapshotted schema USAGE grants as specified in [the maintenance procedure](12_47_MAINTENANCE_CONTROL.md). Require normalized schema ACL equality, preserved old-object grants and exact final 72-function/table/default privilege policy. Keep Vercel protection active during authorized smoke; a grant mismatch is STOP, not permission to open traffic. Using an existing authorized session, load Admin Overview, Calendar, Volunteers, Communications, Quick View and volunteer schedule; verify shared Calendar rendering, ordinary assignment preview, CSV controls without import, hero default with uploads disabled, lunch/away privacy and delivery history without dispatch. Open `https://projectlocal.app/guide` on fresh desktop 1440px and mobile 390px page loads, first through authorized maintenance access and again after restoring the original Vercel protection setting. Require public HTTP 200, `/guide/` and `/guide/index.html` canonical redirects, loaded logo/CSS/JS/fixture images, correct internal section links and mobile navigation, readable annotated screenshots, no overflow or console errors, and descriptions matching the deployed Calendar, sign-in, explicit send and disabled-photo behavior. Check the signed-in desktop and mobile More Contact Guide links. The guide is public and has no authentication dependency; its URL may be emailed to contacts later under separate communications authorization. Do not create or modify volunteers, assignments or profile data merely for smoke; do not send email. Recheck current roster/history fingerprint while accounting for any explicitly resumed legitimate writes. A broken guide asset, route, misleading instruction, runtime error, privacy exposure or contradictory mutation result is STOP before guide distribution or further rollout activities.

## H. Current-terminal backup and expanded-schema recovery

Human approval gate H: authorize a separately controlled post-migration checkpoint at `20260922150000` only after task lock/recovery are GREEN and deployed app is Ready. Require one encrypted artifact, independent checksum, decrypted manifest, no residue and permanent task unchanged. Restore it in a fresh, pinned Supabase-initialized environment with external providers and real email disabled, following [the reduced-scope platform contract](12_47_AUTH_STORAGE_RECOVERY.md). Verify schema, 72-function grants, RLS, essential Auth users/identities, contact/grant relationships, current Storage metadata, operational data and application compatibility. Recreate platform configuration from separate custody; require contacts to sign in again. Never raw-restore unmanaged platform privileges or claim photo BLOB coverage. If expanded-schema restore fails, report a production recovery incident/gate failure and halt further feature enablement/campaigns; do not call SQL rollback a restore.

## I. Optional production photo assets

Default rollout keeps photo uploads disabled. Separate human approval is required to choose and configure primary private object storage, an independent encrypted off-host copy, immutable derivative keys, manifests/hashes, retention/key custody and an isolated loss-and-restore proof linked to DB metadata. Only then explicitly enable the photo feature through a reviewed app/storage change. A database backup alone does not protect BLOBs. If this path remains unresolved, ship only the reduced scope with default hero and keep uploads disabled; record product-owner acceptance of that scope.

## J. Separately approved email proof and campaign

Never infer permission to send from migration, deployment, Communications opening, import or pending recipient count. First obtain a specifically approved existing test recipient and one send operation. Revalidate permissions, eligibility, recipient count, provider configuration and preview server-side immediately before send; send exactly one controlled message, without automatic retry. Check sender, content, single-recipient privacy, existing schedule link, response flow, claim/finalization and absence of duplicate. Unknown provider outcome requires provider investigation before retry. Only after a separately documented owner decision may real volunteer welcome or schedule campaigns begin. Initial sends, explicit resends and failures remain distinct. No bulk sending is authorized by this runbook.

## Abort and recovery decisions

Before B: no mutation; fix evidence, reschedule. B failure: no migration; permanent task unchanged; no retry without new approval. After C but before the first migration: restore task to verified previous Enabled/Ready state through reviewed helper only while DB and lock still equal `20260908130000`.

At each of the three intermediate terminals: if the migration fails, require the DB and lock still match the previous terminal and STOP. If the migration commits but its verification fails, expect old-lock/new-DB `migration_lock_transition_pending`, keep the task Disabled and STOP for investigation; never move the lock just to turn a red check green. If DB verification passes but lock update is interrupted, inspect the **actual** lock read-only: old lock/new DB may be resumed with that one exact reviewed update after operator review; new lock/new DB may continue after all post-update checks. Anything else, including lock ahead, wrong task state, unexpected third terminal, malformed history or a skipped pair, is STOP. No automatic retry. A verified intermediate DB/lock match is a controlled progress marker, **not** permission to enable or execute the backup task. If the next migration cannot proceed, keep writes paused and task Disabled, and choose a reviewed forward correction or proven real restore.

After final migration but before E: do not enable task or deploy until 72-function ACL, data preservation and final matching lock/recovery checks pass. After E but before F: final DB and backup recovery are GREEN; either complete exact deployment or hold traffic/workloads safely until a reviewed compatible source is ready. After F: application rollback is allowed only to an independently verified version compatible with the final schema; it does not reverse database migrations or data writes. At every phase, loss of a matching encrypted checkpoint or ability to independently restore is a STOP, not a reason to bypass backup controls.
